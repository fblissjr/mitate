# Session log — 2026-07-30

Consultation session. **No code changed, nothing committed.** The output is
findings, two owner decisions, and a work list. This log exists so the thread
survives the session — several items below are actionable and unstarted.

## Owner decisions taken this session

1. **`VISION.md` gets the full-destination framing.** The goal is a
   deterministic scene engine — primitives, abstractions, harness — and films
   are how it gets proven, not the product. This **supersedes `plan.md:821-823`**
   ("Scope creep toward a game engine… mitate ships films"); `plan.md` keeps
   architecture and phase gates. Not yet written.

2. **Take the `setCamera(t)` → `setCamera(state{t})` seam now, in the Track D
   batch.** Specified at `working-plan.md:1328-1340`. Bundling it with the other
   Track D items (`STYLE.palette`, `CONFIG.name`/`titleCard`, `hide()`,
   `subjectFromObject`) makes it one 8-carrier cascade instead of two. Not
   started.

## Findings

### Postmortems and the tracked/local boundary

- **`references/materials.md:109` cites `internal/postmortems/2026-07-29_span_instrument-hardening.md`** —
  plugin content, so it ships into every install cache, pointing at a file that
  exists on one machine. Four other tracked files cite postmortems similarly
  (`scripts/sample-determinism.js:4`, `working-plan.md:143`,
  `pattern-ledger.md:42`, `.github/workflows/gate.yml:40`).
- **`selfcheck.js` check 3 cannot see it.** It resolves markdown links only and
  deliberately excludes backticked prose ("Prose may name a repo path; a LINK may
  not leave the subtree"). The exclusion is well reasoned; its premise does not
  cover a path that is not a repo path at all.
- **The privacy objection is empty, measured.** `find-external-paths.sh -d
  internal/postmortems` → exit 0. gitignore is not the privacy control here; the
  hooks are, and tracking routes these through the control rather than exempting
  them.
- **Proposal:** move to `docs/postmortems/`, pin with a root `.postmortem.json`
  (`{"dir": "docs/postmortems"}`) so placement is a decision rather than the
  filing skill's rung-3 inference. Cite from plugin content by **absolute repo
  URL** (the pattern invariant 4 already uses for poster stills).
- **Two blockers in the older file.** `2026-07-25_session_handoff-review.md`
  starts with `last updated: 2026-07-25`, which is exactly the pattern
  `selfcheck.js` check 7 derives its set from — it would fail on first commit.
  It also has no frontmatter, so `postmortem-index` cannot see it. Converting it
  to the `mode/scope/date/summary/artifacts` form fixes both.
- **New rule proposed:** a tracked postmortem may cite a local-only artifact but
  must label it `(local)` and must not rest a claim on it.

### The harness

- **`checkScene` is ~595 lines** (`smoke.js:269`-~862) holding ~11 checks in
  sequence over shared mutable `fails`/`warnings`/`noise`/`dropped`. Each check
  already has its own try/catch and its own name. Recommendation: extract in
  place to `(page, ctx) => ({fails, warnings})` driven from an array — no new
  files, no cache growth, no new parity surface. Argued against
  `templates/checks/*.js`.
- **Bracket coverage: 3 brackets against ~11 smoke checks**, and **zero** on
  `build.js` (827 lines, 18 verbs) or `shoot.js` (327 lines). Highest-value
  missing bracket is **fence parity** — it has two documented near-inert episodes
  (`smoke.js:923-934`, `:946`) and no control. Extraction pays for the brackets:
  `bracket-determinism.js` is 115 lines mostly rebuilding page setup.
- **Live defect — the capture primitive did not reach two consumers.**
  `shoot.js`, `smoke.js` and `scripts/sample-determinism.js` use `seekSynced`.
  **`templates/bracket-determinism.js:33` and `scripts/diagnose-determinism.js:27`
  import `settle` only and still do a bare seek** — the pattern 0.16.28 measured
  at 40%/30%/20% failure on a slow GL stack. So the bracket controlling the
  determinism check uses a race the check no longer has, and
  `diagnose-determinism.js` — which `gate.yml:141` runs *when the determinism arm
  fails*, on the slowest runner — carries the race it exists to diagnose. Third
  site of the failure mode `materials.md` says must never happen.
  **Proposed check:** no file may compare `page.screenshot()` output while
  requiring `backend.js` without `seekSynced`.
- Related: `sha` is defined four times with three different truncations
  (`smoke.js:73`, `bracket-determinism.js:41`, `diagnose:36`, `sample:82`). The
  load-bearing part is the seek → settle → screenshot → hash sequence around it,
  rebuilt four times.
- **`build.js:24` cites `docs/internals/plugin-patterns.md`**, which exists in
  neither this repo nor the install cache. Only such citation in harness code;
  the other seven all point at in-subtree `references/*`.

### Workflows (all three created 2026-07-29)

- **`sample.yml` contradicts itself.** Line 11-12 says `no_canvas: true` must
  return 0/200; lines 35-38 say `no_canvas=true` is the control arm that **must
  still fail**; line 7's own measurement (40%/30%/20% with `no_canvas: true`)
  settles it in favour of 35-38. `sample-determinism.js:69-75` already diagnosed
  this error **and names the workflow header as a carrier**; commit `b233419`
  fixed the script and the input description and left the header standing.
  `internal/log/log_2026-07-29.md` has the same split — line 24 diagnoses it,
  line 61 restates it.
- Root cause: **one criterion written in four places.**
  `docs/source-of-truth.md`'s table covers references, code comments, SKILL.md
  and CLAUDE.md — **not CI config or session logs**, which is exactly where this
  drifted. Proposed: two more rows, not a new section.
- `gate.yml` and `static.yml` are clean. The trigger split is deliberate and
  correct (gate skips prose-only; static is unfiltered and carries `selfcheck.js`).

### Code spill and retrieval contamination

> **RETRACTED later the same day — read this section with that in mind.** The
> noise figures below were measured with shell `grep -r`, which ignores
> `.gitignore`. Ripgrep honours it, so the Grep tool never saw `internal/` at
> all: `solveShot` returns 18 files under shell grep and 13 under ripgrep, none
> from `internal/`. Kept verbatim rather than deleted, because the error is the
> lesson — measure with the tool whose behaviour you are reasoning about.


- Three parallel harness populations under `internal/`:
  `legacy/screenwright/` (a full copy of the plugin subtree), `legacy/explainer-video/`,
  and `the local prototype directory` (own build/shoot/backend/smoke + 9 `bench/` scripts).
  **3 copies of `backend.js`; 21 untracked fence carriers against 8-9 tracked.**
- **Measured noise per symbol** (tracked / staged `site/films` / legacy / the prototype):
  `solveShot` 7/6/14/1 = **75% noise**; `KERNEL-END` 10/6/19/2 = **73%**;
  `buildCharacter` 7/2/8/2 = **63%**; `seekSynced` 5/0/0/0 = **0%**.
  `seekSynced` is the control arm — it postdates every frozen copy, which is what
  proves the mechanism.
- **Correction to an assumption:** neither predecessor is in the `fb-claude-skills`
  marketplace manifest (17 entries, neither present), so `predecessor-record.md`'s
  claim that explainer-video "still ships from the fb-claude-skills marketplace"
  looks stale and `internal/legacy/` may be the only copy. Verify before deleting
  anything.
- **`the local prototype directorybench/` is a promotion candidate, not cleanup** —
  `bench_viewer_{desktop,mobile,loop}`, `bench_playback`, `bench_capture`,
  `bench_reviewloop`, `bench_scene`, `bench_liveplay_{corpus,bracket}`, all built
  on `backend.js`, all aimed at the viewer/playback question the vision needs.
- Proposed `internal/` split **by citability, not topic**: `private/`
  (outside_comms — never citable), `archive/` (legacy, prior_artifacts — the
  noise source), `work/` (log, loose proposals, the prototype). Subfolders fix meaning;
  the retrieval fix is separate and is an exclusion the search path honours.

### Documentation

- **`delivery.md` conflates two subjects**, and its own conclusion (line 197:
  "it ships no recordings at all") contradicts its title. Subject A (encoding a
  recording for a surface that cannot run a scene) carries "Verification date:
  UNKNOWN"; subject B (ship the scene — brotli economics, mount policy) rests on
  this repo's own measured showcase swap. One provenance header, stamped at the
  weaker. **Proposed split:** `delivery.md` keeps the primary path,
  `recordings.md` takes the secondary; "Stills come from the scene" stays in
  `delivery.md`.
- **No tracked router.** The best map of this repo lives in machine-local user
  memory. Proposed a short `docs/README.md` that is only a router
  (question → file → read-it-when), not a summary. The root README's Layout table
  names 3 of 7 docs.
- **Read-window measurement** (the test `working-plan.md:1306` says was never
  run): `method.md` is 996 lines / 52.7 KB and **does not truncate** — the
  correctness argument for splitting it does not apply, and the row falls back to
  its weak taste grounds. `predecessor-record.md` at 2770 lines **does** exceed a
  2000-line default read.

### Doc drift found by the plans audit

- **`film-reviewer` is cited three times in `working-plan.md` as a gate criterion
  at `plan.md:460`.** It is not — `film-reviewer` appears in `plan.md` at `:471`
  and `:509`, both inside DONE narrative, never in a `*Gate:*` clause; the
  examples gate is "owner approval, not rendering." A0 is ranked #0 partly on
  this claim and needs its justification restated on what is true.
- Three stale `plan.md:` line citations from `working-plan.md`
  (`:246`, `:455`, `:704` → the text is at `:471`; `:1732` → text at `:280-283`).
- **Three dangling CHANGELOG versions in `plan.md`** (`:331` 0.69.0, `:436`
  0.74.0, `:503` 0.78.0) — predecessor numbering carried through the repo split.
  This CHANGELOG runs 0.1.0–0.16.29.
- **"Costs only discipline" (`plan.md:109-110`) is contradicted** by
  `working-plan.md:1321-1323`, and the Phase 6 gate text at `plan.md:587-588` was
  left unamended despite being known-unreachable.
- Crushed-exposure carry-forward exists in **three simultaneous states**
  (`plan.md:404-410`/`:424-425` open; `working-plan.md:1533-1539` measured;
  `working-plan.md:191-195` "left undone").
- Examples placement: decided in `plan.md:621-682`, reopened in
  `working-plan.md:1307`.
- `site/app.js` hardcodes `DUR = 16.5`, `BEAT_STARTS = […]` instead of reading
  `window.DURATION`/`BEATS`; `site/index.html:99-109` hardcodes the same
  boundaries a third time as CSS percentages. Three copies, none derived.
- **`internal/site.md` describes a reverted design** (iframe thumbnails deleted
  in `59e6dc2`) and is the only site document in the repo — there is no
  `site/README.md`.

### Vision research

- **The plans do not state the engine goal.** "abstraction" appears **zero**
  times across both plans; "engine" appears three times, all pejorative or as a
  fenced non-goal. But the substrate is built and named: the kernel/driver/contract/
  tooling split (`plan.md:96-110`) and a six-layer promotion ladder with
  cost-scaled triggers (`plan.md:705-722`).
- **The determinism-as-testability argument is genuinely unowned** — confirmed
  independently by two agents that grepped for it and read all three planning
  docs end to end. Nearest proxies: `plan.md:556-560`, `:568-572`,
  `working-plan.md:99-107`, `physics-bake-proposal.md:42-46`.
- **The interactivity/determinism question was already settled**
  (`internal/brief_interactive_viewer_2026-07-25.md:126-142`) and does **not**
  need a prime-directive amendment: a view offset is constant during a frame and
  constant across a re-seek, and `build.js aspect` is the shipped precedent.
- **One fence already crossed the seam:** `gaitPose`/`footTarget` take distance
  travelled, not time. KERNEL's beat layer, SOLVER and DRIVER are the three that
  did not.
- **The instruments would reject an interactive scene today**: `shoot.js:148`
  throws on missing `DURATION`; `smoke.js:437-474` requires rising distinct `t`
  within 5s or reports "live playback is frozen."
- **Where random becomes deterministic:** `scene.template.html:141-142` — seeded
  at module load from `CONFIG.seed`, 4000 draws into a frozen pool, never
  redrawn. Seeded *initialization*, not a seeded runtime and not a build bake.
- **Lost threads from `internal/`, in no tracked plan:** `references/breakdown.md`
  (enumerate the declarative layer; #1 recommendation in two documents; one
  afternoon, no code) and **`build.js check`** (cross-reference validator over
  the tables that already exist; "would have caught at least three of this film's
  defects before a single frame rendered"). Thirteen more classified.

## What shipped (four releases, branch `restructure-2026-07`, unpushed)

- **0.16.30 — R0, truth.** `bracket-determinism` moved to `seekSynced` (run red
  and green either side); `sample.yml`'s inverted criterion given one home; the
  window contract's fourth membership list removed from `CLAUDE.md`; invariant
  6→7 in two places; a `#requirements` anchor dangling since 0.16.13; three
  predecessor CHANGELOG versions; the `film-reviewer` gate overclaim in three
  places; crushed-exposure collapsed from three states to one; `gate.yml`'s
  push+PR double-fire. **`noise1`'s tracks alias** — `4*997 = -12 (mod 4000)`,
  verified empirically before the comment was written.
- **0.16.31 — R1, unrepeatable.** `bracket-parity.js` (five arms, red against
  the real regression). Two `selfcheck` arms, **both spec'd by measurement after
  two wrong specs each**. `source-of-truth.md` gained the comment-boundary rule
  and two rows. `audit-claims` gained `site/index.html`.
- **0.16.32 — R2 part one.** `build.js probe` shipped; `STOP_X` re-derived at
  x -0.3202 / y -0.7606 / z -1.0578, overlap on all three axes. `film-reviewer`
  moved to `plugin/agents/`. Check 3 widened to the whole plugin root.
- **0.16.33 — R2 part two.** Postmortems tracked in `docs/postmortems/` with
  `.postmortem.json`; `references/glossary.md`; map blocks in `CLAUDE.md` and
  `SKILL.md`; `docs/README.md`.

**Verified at the end of R2:** smoke 6/6, brackets 4/4, parity green, selfcheck
green at 14 dated docs.

## Four times measuring beat reasoning, and one of them was mine

Worth carrying forward, because the pattern held every time:

1. `diagnose-determinism.js` was accused of a capture race from its **import
   list**. Its `gridAt` seeks and reads back in one evaluate. Not guilty.
2. The seek check's first two specs each condemned correct files — one of them
   the deliberate control arm.
3. The citation check's broad spec reported **46 failures, 45 false**.
4. **The `internal/` retrieval finding was wrong.** "63-75% grep noise" was
   measured with shell `grep -r`, which ignores `.gitignore`. Ripgrep honours it,
   so the Grep tool never saw `internal/`. The item was retracted, not deferred.

## The plan

Everything above is sequenced in **[`docs/restructure-2026-07.md`](../../docs/restructure-2026-07.md)**
(tracked, disposable, retires when its last gate is green). Six phases ordered
truth → reachability → structure → capability. Read that, not this log, for what
to do next.

## Late finding — instance 7 of the contact class, inside a shipped example

`examples/bear-and-bees.html:1375` reads
`const STOP_X=-1.2;  // solved from the probed nose reach (see probe.js)`.
**`probe.js` does not exist** — not in `templates/`, not anywhere in the tree,
and `build.js` has no `probe` subcommand. `SKILL.md:269` and
`examples/README.md:57-59` both advertise "probe-measured contacts". So the one
number that makes the flagship gag geometrically true cites, as its provenance,
an instrument that was never committed. Second instance of the
claims-a-check-exists class after `solveShot`'s extent-check comment.

Also found: `bear-and-bees.html:1379`'s erupt recoil (`-.38*pulse(t,'erupt',.12,1)`)
runs while `vAmp` is 0, so `gaitPose` collapses fully to the body-relative rest
stance and **all four paws translate rigidly with the recoil** over a 1.1s beat
in an FSA shot. Unrendered; `build.js strip` over that window would settle it.

## Open threads

- `VISION.md` — approved, not written (R3).
- The Track D batch including the state seam — approved, not started (R5).
- Whether `internal/legacy/` is the last copy of either predecessor — blocks
  R2.5's exclusion decision.
- Whether `sample-determinism.js` is spent now that its forward item closed.
- Whether the erupt-recoil foot slide reads on screen.

## Dependency changes

None.

---

# Session part 2 — R3 close-out, R4 expansion (14:10 onward)

Continued after a context compaction. Range `e530631..e7b5a34`, versions
0.16.37 through 0.16.40.

## What shipped

- **0.16.37** — closed the two open review findings (citation check compared
  basenames not paths; its accept-set came from the live filesystem so it
  differed between laptop and CI). Fixing them exposed two more: nothing ran
  `bracket-selfcheck.js`, and `static.yml`'s parity step carried a stale claim
  plus a dangling `\` left by 0.16.35. New `bracket-stage-films.js`.
- **0.16.38** — split `delivery.md` / `recordings.md`. One provenance header had
  covered inherited encoder measurements (honestly UNKNOWN) and this repo's own
  brotli figures (measurable, 2026-07-24). Eight pointers followed.
- **0.16.39** — `working-plan.md` prune: verified status column on the sequencing
  table, two superseded positions struck rather than annotated, ancestry row for
  `probe` corrected. Trimmed `CLAUDE.md` 248 → 209.
- **0.16.40** — `site/` framing corrected twice (see below); site language
  aligned to `VISION.md`/`README.md`; check 6f brackets the prime directive's one
  exception.

## R3 MET

Cold-start run: 1 hop, ~940 lines, answer in the first 24 lines of the live
queue, no superseded document read unwarned. It found three defects, all in the
verification layer — three stale doc-to-doc line anchors (shifted by 0.16.39's
own prune), two stale hand-written counts inside the document that states the
never-hand-write rule, and `docs/orientation.md` unreachable from either router.

The byte clause was retired by owner call in favour of "no rule lost, no line
unearned", verified by term diff.

## Owner corrections worth keeping

1. **`site/` is a side thing** — a website, a glorified README with example
   scenes. I had inflated it to a "capability-claim surface" and a "vision
   carrier", partly by repeating a review agent's framing without checking it.
   Then over-corrected to "nothing depends on it", which was wrong the other way:
   nothing flows back, but the site *derives* from VISION/plan/README. Audit
   found it had reached one load-bearing place (a `source-of-truth.md` row) and
   no code.
2. **Do not explain a content decision in a public repo.** I removed the
   reasoning three times and left a softer version each time. Also had to rewrite
   ten unpushed commit messages (tree byte-identical) because it had gone into a
   commit title.
3. **Site length framing** — short because a project site should not ship a giant
   cache of code, not because anything caps duration.

## R4 is now seven items with an explicit order

Added 4.4 `--parity-fix` (4,611 lines held identical by hand), 4.5 the defect
corpus (built from the prototype with new theme/character/name/title font/style, script
and captions kept), 4.6 retention, 4.7 portfolio grading. Start-here block gives
the order and the reason for each position.

## Findings recorded elsewhere

- Postmortem: `docs/postmortems/2026-07-30_span_uncontrolled-controls.md`
- Memory: three conflicts fixed (posters embedded by absolute raw URL not
  relative — the memory stated what invariant 4 forbids; postmortems moved to
  `docs/postmortems/`; `probe.js` never existed in either predecessor). One new
  memory on verifying a subagent's *framing*, not just its findings.

## Open

- **The structured-data question was recorded nowhere** until today — a design
  discussion with an owner position that survived only in a transcript. Now an
  open question under R4.7.
- PR #2: body rewritten, six threads posted. **Six cited SHAs changed** in the
  message rewrite and need repointing after the next push.
- Everything since `052860b` is unsigned (1Password unavailable).

## Dependency changes

None.

## Tail — push, CI, and the fourth instance (evening)

Re-signed the 12 unsigned commits (`git rebase -f -S origin/restructure-2026-07`);
all 46 on the branch verify `G`. SHAs moved again — owner's call: the six stale
citations in the PR threads stay stale, recorded under "deliberately not doing".

**`static` failed on its first two pushes**, and the defect was in the bracket:
`bracket-selfcheck.js` wrote a fixture into `site/films/` without creating it, and
a clone has no such directory because `films/*.html` was gitignored so git stored
no empty dir. Reproduced locally with `rm -rf site/films`. Fixed at the root with
a tracked `.gitkeep` (owner's suggestion, and the better fix — my `mkdir -p` was
patching three callers) plus widening the ignore to `films/*` so a staged
non-html artifact cannot be tracked by accident.

`gate` + `static` both green on `63200fb`.

Also deferred, both recorded with triggers: mechanical coverage for `.claude/`
(or decoupling those rules into their own repo), and repointing the moved PR
hashes.

## Follow-ups

- Merge PR #2, then `/plugin install` + `/reload-plugins` (closes R2's last
  unverified half).
- Site copy only reaches production on merge — Netlify builds `main`, and the
  eight `index.html` edits sat on the branch, which is why the live page still
  read "a pure function of time".
- R4 item 1: `templates/bracket-commands.js`.

## Tail 2 — merged, R2 verified, R4 started

**PR #2 merged** (`bfae322`). `main` at 0.16.40 with the site copy; Netlify built
from it. Branch `restructure-2026-07` deleted local + `origin` + `nas` — fully
merged, 0 commits not in main. **The branch is gone; the migration is not.**
`docs/restructure-2026-07.md` stays live until R5's gate.

**R2's deferred half verified rather than assumed.** Measured the real install
cache after `/plugin install`: it holds `agents/film-reviewer.md` (the reload
registered `mitate:film-reviewer` as a live agent type), holds `agents/`,
`skills/`, `README.md` and *nothing else* — so invariant 3 is observed, not
asserted — and is byte-identical to `main`'s `plugin/` subtree apart from an
`.in_use` marker.

**R4.2 + R4.3 DONE (0.16.41)** on branch `r4-harness`, off main.
`templates/bracket-commands.js`: 13 verbs + 4 red arms, 38s, skips reported and
excluded from the tally. **No CI edit** — `gate.yml`'s existing
`templates/bracket-*.js` glob covered it, which is cheaper than the step R4.3 had
prescribed.

Its first run failed and found a constraint, not a bug: `vendor` shells out to
`bun build`, which resolves `three` from the **entry file's** directory, so a
fixture in `os.tmpdir()` cannot vendor even with three installed.
`require.resolve` in `build.js` falls back to cwd and is satisfied; the bundler
is not. Fixture now lives inside the invoking workspace.

That is two brackets in two days finding a real defect on their first unattended
run — the argument for the tier, made twice.

## Next session

1. **Push `r4-harness`** (only action outstanding; the work is committed and
   signed but local). Watch `gate` — `bracket-commands.js` has never run there.
2. **R4.4 `--parity-fix`** — the next item, and deliberately not started at the
   end of a day: it edits `smoke.js`, needs a bracket arm for the
   malformed-source refusal, and touches the parity machinery that has twice
   gone quiet while printing ok.
3. Order and reasons are in R4's **Start here** block.

## Dependency changes

None (workspace `bun add` was scratch-only).
