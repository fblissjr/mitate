# Session log — 2026-08-02

Narration, not doctrine. Started from a cold "I can't remember where we left off"
and ran to a rewritten `VISION.md` and R5.2 closed. Six versions merged or
shipped: 0.16.61 and 0.16.62 landed, 0.16.63 through 0.16.66 written today.

The through-line, unplanned: **every audit pointed at something eventually found
one more instance of the class it was auditing.** That happened four times and
twice the instance was in the auditing instrument itself.

---

## The merges, and the thing neither PR predicted

Verified both open PRs against the tree rather than trusting their bodies. Every
checkable claim held — `onThrow` per check with a hard error on undeclared, the
`!fails.length` guard gone, `setCamera(state)` across eight carriers, five of
seven `gaitPose` call sites relying on the default, the ledger's ×7 actually ×11.

**Then dry-ran the merge, and it turned `main` red.** Check 11 anchors on the
last commit touching `plugin.json`; a merge commit taking one side's version is
TREESAME to that parent, so git skips it, the anchor lands on the branch bump,
and 0.16.61's `smoke.js` reads as unversioned plugin content. Squash-merging
trades the failure rather than fixing it — cascade goes green, freshness reds on
every doc dated the previous day because `%cs` becomes the squash date.

Fixed by carrying the **0.16.63 reconciliation inside the merge commit**, which
makes it non-TREESAME to both parents and therefore the anchor. A follow-up
commit would have worked in principle and was unlandable in practice: the
pre-commit hook runs `selfcheck`, so the intermediate state could not be
committed without bypassing a hook.

Neither PR's CI had covered the merged combination — one changed the checker, the
other changed the scenes. Ran it: smoke exit 0, `bracket-driver` 18 arms 0
skipped. On Metal, not CI's WebGL2, so corroboration rather than substitute.

**Also restored the ninth sitting to yesterday's log**, which existed only on the
trio branch. An agent had inferred that gap; the merge proved it.

## Corrections, and one that was wider than reported

The ledger's presence-gating row said the `Math.max(1e-4,…)` spelling was
local-only. It is in `fixtures/defect-corpus/after-hours.html`, tracked, at
exactly ×11. 0.16.62 had reasoned from the wrong label to decline promoting
`hide(obj,u)`. The count does not change — a re-skin is the same instance carried
into the tree, not an independent rebuild — but the row rests on nothing local,
which is what the label is for.

The site and `README.md` both printed `setCamera(t)` in an excerpt captioned as
coming from a named shipped file. Grepping for the old signature then found **six
present-tense claims across four planning documents** still asserting the seam
did not exist. The consequential one is `plan.md`'s Phase 6 gate, which said
"unreachable" and named exactly why — the seam removes that obstacle, so the gate
moved to **untested**, which is a different state and a better one. Corrected
without claiming it met; only a spike can measure that.

## The snapshot, and six agents

Built `snapshots/2026-08-02/` as a new top-level tier — a dated record in the
same class as the changelog, framed hard as settling nothing, because a document
titled "complete snapshot" is the most dangerous shape the second-copy failure
can take here.

Dispatched six subagents across four investigations: the changelog arc, the
session logs, the commit record, PRs plus postmortems, then the foundation
analysis and the relational question. **Verified before using**, and it mattered
twice — one agent's "signing outage is open" had closed by the time I read it,
and one inference about a missing log section was resolved by the merge instead.

What they found that I checked and kept:

- **Zero reviews and zero inline comments across all six PRs.** PR #1 merged 77
  seconds after opening with a stated purpose those 77 seconds cannot have served.
- **The chain nobody had connected:** `2c5742f` — subject *"a test-audit of the
  only test suite, and what it found green"* — introduced **both** the
  `!fails.length` guard and the console-filter defect that failed the entire
  shipped corpus for seven releases. And that commit was the execution of the
  first postmortem's forward item. Each half had been traced separately by
  different sessions; nothing joins them.
- Product versus meta yield across the whole apparatus: **4 against ~25**.
- 91% of `scene.template.html` still blames to the migration commit; `smoke.js`
  is 26%. The harness has been rebuilt; the representation never has.

## Four defects, one shape

The relational audit's verdict was *the instinct is right about the gap and wrong
about the shape* — membership and one join, derived rather than stored. Then it
found four live instances of the one thing a scalar marker structurally cannot
express:

- `smoke.js` had **two copies of the fence list**, and `derived-counts.js`
  scraped the `--parity-fix` copy while `--parity-only` enforced the other. The
  instrument whose stated guarantee is that it "cannot miss and cannot
  false-positive" was deriving from the wrong array. Hoisted to one const —
  deleting the copy is O(0), guarding two is O(n).
- `glossary.md` and `bracket-parity.js` both still said six fences, twenty
  versions after `CONTRACT` became the seventh. Both **ship**.
- The parity comment's "4,611 lines held byte-identical" was stale by ~24%; true
  figure 5,704. `--parity-only` now derives and prints it.

Earlier the same day: `selfcheck.js`'s encoder comment read `10 -> 6` against a
five-entry table. **That one sat between checks 12 and 13 — the two built to kill
stale counts — in the gap neither reaches.**

## VISION.md

Rewritten whole rather than appended, on the owner's call, then named as the most
important document in the repo across four routing surfaces. Four things it did
not say: where a declaration lives; that `state` is the intermediate layer and
the window contract is not; that reproducibility and byte-identity are different
claims and only the first is load-bearing; and what shape a primitive has to have.

The owner's framing, recovered rather than invented — the repo's own pre-mitate
record already carried **"data and a small compiler, not an abstraction layer"**,
and `restructure-2026-07.md` records the 2026-07-30 discussion of it as the
worked example of knowledge evaporating. It was still absent from `VISION.md`
three days later.

Two success criteria were added that are **currently false**, and the file says
so: nothing validates a declaration before rendering, and no determinism failure
reports a magnitude.

Also fixed a real routing gap — `orientation.md`, the briefing handed to
subagents who never auto-load `CLAUDE.md`, did not mention `VISION.md` at all.
The reader most likely to propose something structural got the two rules with no
route to why they exist.

## R5.2 — the enumeration

`references/breakdown.md`, derived by reading the templates and every example
rather than the plan. The `STYLE`/`CONFIG` key surfaces were enumerated
mechanically, which is why the kit-versus-film split is known rather than guessed.

The finding that reorders what follows: **validation clusters where a mistake is
unrepresentable, not where it is expensive.** Unknown names throw because a lookup
fails; an extent that does not match its geometry, an anchor outside its beat and
a caption that will not fit have no check — and all three are decidable from the
tables before a frame renders. That is the gap a compile step closes and a runtime
cannot, and it arrived from measurement rather than advocacy.

Also: `STYLE` has twelve kit-read keys against three declared in the template.

## What I got wrong

- **Crashed `selfcheck` while adding a check to it** — called `.filter` on a
  `Map`. Identical shape to check 13's 0.16.59 crash: reports nothing and blames
  the wrong file. Caught by running it.
- **Said "8 of 9 brackets" print no derived count.** Wrong; it was 3, as I had
  originally measured. A grep false-positived on unrelated interpolation and I
  reported it before checking the output.
- **Conflated shipping with building** in the snapshot — wrote that no new film
  has shipped, then editorialised "a project that stopped making films" on top of
  it. Scenes have been built and tested throughout. The owner corrected it. That
  is the adopted-framing failure, committed inside the document describing it.
- The ratchet caught me **three times** for asserting a measurement without
  naming a control. Each time the fix was to name one.


## Later the same day — R5.3, `build.js check` (0.16.67)

Picked up the item the block above names as next. Shipped four of its six
work-list items, blocked one on the prime directive, and found the sixth does not
exist.

**The routing question came first and decided everything else.** The work-list
needs `SHOTS[].at`, `subject`, `focus`, `size`, `SUBJECTS` keys and the `SIZES`
ladder. Only `{t, cutEnd}` of that is on `window`, and SUBJECTS/SIZES are on no
window at all. Three options: widen the contract across every carrier, reach into
scene internals through `page.evaluate` (a second probe exception), or read the
source text. Took the third. It drives nothing, so the prime directive's clause
does not bind it; it knows only kit-owned table names, never a film's own
identifiers, which is the property the clause exists to protect. And it works on
a scene too broken to load — which matters, because a shot naming a nonexistent
beat throws inside `beatAt` at load, so a contract reader would have nothing to
inspect on exactly the input that most needs checking.

**Then it condemned a shipped film on its first run, exactly as predicted.**
"Union shots use only wide rungs" fired on `bear-and-bees.html`'s two-shot —
which is deliberate, annotated in place, and asks for `MS` on a pair with
`anchor:.45`. Reading the solver settled it: an explicit `anchor` overrides the
rung's own, so that shot supplies precisely the landmark a union box lacks. The
narrowed rule is silent across the whole corpus. Without the narrowing the verb's
first output would have been a false accusation against a correct example, which
is this repo's oldest check-authoring failure and it took ten minutes to walk
into again.

**The corpus fixture is a thinner positive control than expected, and that is
information.** `after-hours.html` is clean against every on-spec check. Its
declarative defects are #11 (declared extent, the blocked one) and #10b (repeated
framing), so a directive-respecting checker reaches exactly one of them. Added
the repeated-framing warning for that reason and said so — it is beyond the
work-list, decidable from the table, and the only thing on this fixture a text
pass can see. It also re-measures 10b: **four** identical framings, not the five
the prototype's squint strip reported. The fifth differs by `elev` alone. The eye
saw a repeat; the table sees a different shot.

**Mutation-tested all fourteen new arms** — each check neutralised in a mirrored
`build.js`, each arm confirmed green→red. The union pair was additionally run
end-to-end through the harness with the narrowing removed: the anchored fixture
starts warning, the `absent` arm reports FAIL, exit 1. That `absent` assertion is
new machinery and it is the half that catches a check firing on a correct file,
which is the failure this verb was most likely to ship.

**Two smaller findings.** `breakdown.md` had missed `anchor` and `anchorX` from
its `SHOTS` field table — both read by the solver, both used by shipped examples;
found by writing code against the spec rather than reading it. And R5.3's
work-list contained an item that cannot exist: `TOTAL` is derived from `BEATS`,
so they cannot disagree, and `breakdown.md` said so one tier above the row that
asked for the check. A plan does not re-read itself when its prerequisite lands.

---

## Then the audit, and it found ten things

Ran `/audit-claims` over the whole branch — the first time it has been pointed at
a day's output at scale. Four auditors in parallel, each given **explicit quoted
claims** rather than a directory, because the skill is blunt that a vague scope
returns a vague answer. It was right about that: the auditor handed a list of
sentences plus "test this empirically" came back with a code defect; the same
instrument pointed at a folder would have come back with prose.

**Ten drifts. Nine written today.** The distilled version is in
`docs/postmortems/2026-08-02_session_audit-of-one-days-output.md`; what belongs
here is what it felt like.

The one that is not drift: **`build.js check` reports `ok` on a table it could
not read.** A loop-built `SHOTS` gives `0 shot(s)` and a green verdict, no
warning. That is the same silent-scope shape I closed for the brackets this
morning, shipped again in a validator hours later — and I only saw it because an
auditor built an adversarial input instead of reading the code.

**Three of the ten came from trusting a derived answer instead of the source**,
which is the through-line and it is mine: a grep capped with `head -6` whose
truncation I read as completeness; a grep that false-positived and displaced a
correct number I had already obtained by running the thing; and a figure carried
out of an agent's report into a tracked document without recomputation.

Also caught: the `count-mention` escape hatch used to **exempt a wrong number** in
the corpus README, and `VISION.md` asserting a success criterion "currently
false" that a commit two versions later had made mostly true. Both in files I
wrote today; the second in the file I spent the afternoon declaring authoritative.

Fixing nothing in that pass, per the skill — reporting and repairing in one motion
is how a wrong diagnosis becomes a wrong edit. The findings are grouped in the
postmortem's forward items: (1) with (4), (5) and (9), because each describes what
`check` covers; the rest separately.

**And the audit was itself undocumented for an hour.** It lived only in a
conversation until this section existed — the exact failure
`restructure-2026-07.md` files as its worked example, hit for the third time
today, while fixing it elsewhere. The handoff written an hour earlier was already
stale for the same reason.


---

## And then a code review, which out-yielded everything else

Pointed deliberately narrow: not the diff, but `build.js check`'s **reimplemented
semantics** against the fences it mirrors. `check` is static, so it cannot call
`beatAt` — it rebuilds beat accumulation and anchor resolution itself, and
`plan.md:772` already names that trap: *a control that rebuilds its subject
verifies a reimplementation.*

**Fifteen findings.** More than the claim audit (nine) and the adversarial input
(one) combined. Nothing had ever pointed a review at a control-versus-its-subject
here, and it is now the highest-yield instrument tried.

Two fixed on the spot (0.16.70):

- **A false ERROR on a valid scene.** `dur: SOME_CONST` is ordinary authoring and
  the unresolved-identifier proxy made it read as invalid — exit 1, plus a message
  quoting a value the source never wrote. Live in CI, because I had wired `check`
  into every push a couple of hours earlier. My own step, my own risk.
- **The silent-green class was still open.** 0.16.68 closed it for loop-built
  tables and I wrote a commit message saying the class was shut. A table whose
  literal cannot be *sliced* took the other door: `no SHOTS (2D)` and a clean
  green on a 3D scene. Third time today that fixing the instance left the class,
  and this time inside the fix that claimed otherwise.

The other thirteen are filed. They are all one shape — `check` and the kit
disagree, in both directions, and nothing compares them.

---

## Position, first session (superseded by the second session below)

`main` at **0.16.70**, everything merged and pushed, no branches, no open PRs. `selfcheck` green at
nineteen checks, parity ok over nine files and 5,704 held lines, all brackets as
specified.

**Next, in order:** the thirteen open review findings, whose common root is that
nothing compares `check`'s model to the kit's; the representation design session,
whose brief is written; `bracket-corpus.js`, since the corpus is read in CI now
but not gated; the yield table against diffs; `motion` off the encoder.

**This block moved.** It was stranded mid-file with two later sections appended
below it — the shape this repo names as burying a summary rather than correcting
it. Kept at the end from here.

**Not chasing:** the eight `selfcheck` checks with no bracket arm. Its own
falsifier applies — some are probably correctly unarmable, the way invariants 5
and 7 are.

---

# Second session, same day — the representation decision

Picked up from the handoff; its lead held against `git log` this time. The work
item was the representation design session, and it ran the way the brief's
guard asked: written positions compared, not a live convergence.

## How the session actually ran

A draft position went to a local draft document first
(local, untracked). The owner redirected mid-draft — long-term strategic
decision, do not over-fit the current code — and the draft was rebuilt around
VISION's destination with the shipped code demoted to evidence. It then became
`docs/representation-exploration.html` on branch `representation-exploration`:
the argued page, with examples, tradeoffs, and an R0–R6 roadmap.

The page was audited against `VISION.md`, `plan.md` and the site before being
adopted, at the owner's ask. Three substantive corrections came out of that:
the movement rule had reintroduced the exact single-trigger defect `plan.md`'s
Promotion section names; the extent fork was presented as evidence-free when
the plan records hand-declared extents wrong three of five and the ledger holds
the class at count 6; and the tailWag exhibit sat on VISION's Phase 4 hit list.
The cost split got measured instead of asserted: menagerie's seven fence
regions are 823 lines — 3 declaration literals, 458 machinery, 341
comment-only, 21 blank — so the parity tax funds emission, not schemas. A
late owner question added the harness-language position: stay JS-family,
TypeScript decided at the seams in R2, other languages only behind
derived-data boundaries.

## What was decided and recorded

Owner adopted all four options put to them: the boundary as argued, extents as
derive-plus-intent-channel, durable foundation as the strategic frame, and the
R1 spike now. The decision section in `docs/representation.md` is written —
the file the brief created for exactly this. `VISION.md` now points at it and
carries the measured fence composition; the restructure plan's Open question
is marked resolved; the site's bootstrap hedge is reworded.

## The spike

`scripts/emit-spike.js`: all nine carriers reassemble byte-identically from
film skeleton plus a canonical fence store (7 fences, 837 lines). The R1 gate
is reachable. The vendor blob is named out of scope on purpose.

## The audit, and the day's recurring shape

`/audit-claims` ran over the whole diff plus `.claude/` and `.github/` — six
auditors. Clean tiers: every measured figure re-derived, `.claude/` briefings
all true. Eight findings elsewhere, all fixed and pushed:

- **Five same-day stale-framing drifts**: VISION's method pointer, the page's
  banner/R0 gate/footer still calling the session open, the router rows, and
  an R2 gate citing a finding recorded only in the untracked handoff. The
  shape of the day, again: the decision landing made prose written hours
  earlier false. Fixing instances does not close positions — this log said so
  this morning about checks, and the afternoon demonstrated it about prose.
- **Two `.github/` comment drifts**: `static.yml` claimed its new step was the
  first thing in CI ever to read the corpus (fence parity had read it since
  0.16.45 — claim narrowed to what is true), and carried a cost figure from
  before two of its five steps existed (replaced with the dated whole-job
  range from today's four runs).
- **Four spike gaps from the adversarial pass**, each proven red on a scratch
  tree before fixing: silent green over a partially-empty scope, a crash
  blaming the wrong line on an all-empty scope, uncaught stack traces on
  malformed carriers, and duplicate fence names accepted. The `mismatched`
  exit term was kept against the auditor's "dead code" verdict, with a comment
  saying what it actually guards.

## Position, end of day

**Merged.** PR #8 (`emitter-spike`, which contained `representation-exploration`)
went into `main` at end of day — merge commit, not squash, per the check-11
lesson above; gate green on the PR before merging; branches deleted after. The
decision is recorded. The spike says R1 is real. (This block first said
"nothing merged — merging is the owner's call", was true when written, and was
made false by the owner's call an hour later; corrected the same evening,
which is the day's stale-framing lesson applied to its own log.)

**Next, in order:** R1 proper — the emitter as a real tool with a bracket, the
parity inversion, and the site's source-equals-artifact wording in the same
phase; R2 — `check` reads the canonical source, the thirteen findings close as
a class, TypeScript-at-the-seams decided there, with the standing trigger: if
R1 stalls a week, build the forward-item-5 comparison bracket instead;
`bracket-corpus.js`, still unbuilt, still the standing embarrassment;
then R3/R4 per the page.

# Third session: the emitter phase lands

Picked up from the handoff, lead verified (one predicted rot: HEAD was the
post-merge log commit, not the merge commit itself). Ran the spike before
trusting its green, per instruction: 9 carriers, 7 fences, 837 canonical
lines, both arms fired.

## What shipped (0.17.0)

The fenced kit is single-sourced and parity is inverted. In order, with the
red proven first:

- **Store extracted**: `templates/fences/<NAME>.fence.txt`, seven files,
  block-verbatim plus one trailing newline, pulled from
  `scene.character.template.html` (parity green over all nine carriers is the
  proof the source choice does not matter — 837 lines, matching the spike's
  canonical count exactly).
- **Bracket rewritten first, red witnessed**: 27 of 31 arms failed against the
  pre-store `smoke.js`, every one with WRONG-MESSAGE — the message asserts are
  what make the red genuine rather than incidental nonzero exits. The four
  passing arms were the ones whose behavior legitimately does not change.
- **`smoke.js` inverted**: one `fenceRe` (was two inline copies; the loader
  would have been a third), `loadFenceStore` refusing missing/partial/extra/
  mangled stores with all problems reported at once, `--store <dir>` for
  brackets (default resolves beside `smoke.js`, so the install cache stays
  self-consistent), `--from` refused with the reason, `--parity-fix` now
  regenerates from the store and reports the fences that actually changed.
- **The phase gate, met**: `--parity-fix` over all nine carriers said nothing
  to do; tree byte-identical; `--parity-only` reports the same 5,704 held
  lines as before the change. Zero behavior change, then the semantics moved.
- **Wording moved in the same release**: SKILL.md (three spots + provenance),
  `glossary.md` parity-set entry, `instruments.md` row + fence paragraph,
  `install-hooks.sh` descriptive comment (HOOK_BODY untouched — check 8
  quiet), and the site's "the one scene file stays the source" / "diffs like
  source" sentences. CLAUDE.md invariant 2 deliberately NOT touched at the
  time: another session held docs/** and CLAUDE.md, so the wording need was
  handed over instead. (Landed after that session finished — its pass left
  invariant 2 for "REP1's gate", the gate went green on this branch an hour
  later, and the claim lapsed with the pass. Written when the first framing
  was still in this file: the stale-framing grep caught the log itself.)

## Traps hit

- The assertion ratchet caught the word "measured" in a bracket comment that
  was not a measurement claim (52 > 51); reworded to "judged" rather than
  spending budget. The scanner cannot tell the difference, and that is fine —
  the cost of a synonym is zero.
- Piped selfcheck into `tail` while iterating and read tail's exit code —
  the exact `feedback_check_exit_codes` lesson; caught it in the same breath,
  re-ran unpiped before trusting anything.
- `bracket-selfcheck.js`'s must-stay-green arms report DIRTY on a mid-change
  tree (they run repo-wide selfcheck expecting green). Not a defect — verified
  the two CAUGHT arms behave, and CI re-runs it on the clean checkout.

## Coordination

Shared checkout with a second session working docs/** + CLAUDE.md. Split held:
this session claimed plugin/**, site/, scripts/install-hooks.sh, and the
cascade files; staged by path; the other session's staged doc edits left
staged and uncommitted. Per its request, durable text here says "the emitter
phase" rather than the roadmap label, which is being renamed under it.

## Addendum: the .github/ miss

The owner caught it: neither session swept `.github/` after the inversion.
It was not prose drift — gate.yml's workspace step copies smoke.js without
`fences/`, and SKILL.md's documented setup has the same shape, so both the
gate and the documented authoring loop would have died on the store refusal
(reproduced in a scratch workspace before fixing; green after the copy list
gained `fences/`). static.yml's parity comment also carried the old
cross-carrier rationale plus two stale hand-written facts — "all five
fences" (six since CONTRACT, 0.16.44) and line indexes from before the file
grew; re-verified mechanically, indexes dropped rather than refreshed.
Shipped as 0.17.1. The lesson is the checklist shape: the 0.17.0 sweep
covered every place that DESCRIBES parity and missed the two places that
BUILD the workspace it runs in.

---

## Day closed: PR #9 merged

`a0f50c2`, merge commit, gate green on the PR tip first (6m33s), branches
deleted, `main` at 0.17.2 with selfcheck green post-merge. Last acts before
the merge: the exploration page brought current (where-this-stands box, REP1
met marker, four stale-present-tense fixes), the site roadmap gained the
parallel track and then had it moved to where parallel things sit, and the
emitter session's postmortem landed and rode the final push. Still queued,
now for the next session: emit-spike retirement via the retire sweep,
bracket-corpus.js (six PRs), REP2.
