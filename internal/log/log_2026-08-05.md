# 2026-08-05 — the cold session comes home: mining market-crash, shipping 0.19.4

The owner pointed this session at the artifacts of the 2026-08-04 cold-start
build — the market-crash film built in an isolated workspace outside this
repo, by a sonnet-5 session running only the installed plugin (0.19.3, which
was byte-identical to the working tree at the time, so it exercised exactly
what ships). Evidence base, all local and untracked: the scene and its two
postmortems from that build `(local)`, the build session's transcript
`(local)`, and a local database that indexes it `(local)`. One caveat on
the window: thinking blocks in the raw transcript are stored empty, so the
behavioral evidence is the session's visible narration plus its tool calls —
46 thinking blocks exist and none is readable.

## What the transcript shows (derived, this session, from the db)

- Prompt to delivered MP4 in ~18 minutes (19:32:08 → 19:50:28), 96 tool
  calls, **zero tool errors**, zero user interventions between prompt and
  delivery. The session-level rollup records `total_tool_errors = 0`.
- The session read exactly two references — `method.md`, `bibles.md` — plus
  the 2D template before writing the whole scene, and **opened no example
  scene at any point** (every Read in the db is accounted for: the scene
  itself, build.js, the instrument output images). That is
  examples-placement's settling experiment, effectively run: examples were
  in the cache and did zero teaching for the 2D register.
- Its task list mirrored the documented loop exactly (check → composition →
  continuity → semantics → smoke/deliver), unprompted.
- The instruments caught two real defects pre-delivery: `sheet` a duplicated
  HUD readout at the halt; `motion` the halt beat as the film's
  highest-motion beat (10.73 vs 5.20 next) — backwards for a freeze — fixed
  to 7.40 with a measured dead-air hold (t=32.00–34.17s, 44% of the beat).
  Red-first happening in the field without a rule making it.
- The genuine escape: the halt banner's blink, `sin(quant(t,2)*Math.PI*2)`,
  exactly 0 at every `t` from the first draft. Pure in `t` (smoke green), no
  discontinuity (no strip pop), sub-threshold for `motion`'s whole-frame
  delta. Found by hand, by accident. The cold session's own postmortem
  correctly called it a toolkit gap, not a discipline failure.
- Near-misses and friction: the HUD was first hand-rolled with
  `ctx.fillText` (silent `?strip=text` opt-out; caught by re-reading the
  template's kernel comment, never by an instrument); `build.js all` left
  1,110 PNGs / 52 MB in `frames/` unnoticed until the postmortem (figures
  re-derived from the transcript's own `ls`/`du` output, not from prose);
  one wrong-file read on the `nocap` sheet name; three `strip` runs
  overwrote one fixed output filename.
- Two instructions did not bind: SKILL.md step 4's "Delegate this to the
  `film-reviewer` agent" (the session reviewed inline — and shipped fine),
  and the film field report fired **by name only** — the session announced
  the step, then wrote a delivery note without the three bullets.

## What landed, and where

- **0.19.4** (cascade complete: plugin.json, marketplace.json, changelog).
  `method.md`: txt()-from-first-draft + early-nocap rules in the semantics
  axis; new continuity section "A composed periodic can be constant";
  provenance amended. `instruments.md`: the no-instrument ledger gains the
  txt() bypass and the inert sub-expression, provenance dated. `SKILL.md`
  step 7: the `frames/` cleanup note, provenance dated.
- **`docs/README.md` work-next row rewritten**: market-crash landed, VISION's
  docs-only test ran for real, and **executing examples-placement option E is
  now the front of the queue** — the decision's batch condition ("the first
  new portfolio film") fired 2026-08-04.
- **`docs/examples-placement.md`**: the landed note in the decision block,
  and the zero-example-reads evidence under the settling experiment (one
  build, one register — 3D/character untested, said so).
- **`docs/pattern-ledger.md`**: the field report's second, partial firing
  graded precisely — fired in name, form not followed, no row increments.
- **`docs/working-plan.md`**: the option E deferred row closed as
  decided-and-fired; three new filed rows (mechanical inert-expression
  check, `fillText`-outside-`txt()` detector — both routed through
  controls.md's door when built, each with its natural red arm named —
  and the output-naming friction pair); Owner's call 5 filed on the
  film-reviewer delegation wording.

No new pattern-ledger shape rows: the film consulted no examples, rebuilt no
counted shape, and its field report named nothing built twice — a graded
zero, not an omission.

## The brainstorm consumed itself

`docs/brainstorm-2026-08-04.md` (and its html rendering) deleted this
session per its own graduate-or-die rule: with the 2026-08-04 owner
decisions recorded in it and market-crash landing the first step of the
section-2 ordering, every item carried a disposition except the gate-cost
watch threshold, which was still marked "proposed" — graduated to
working-plan's deferred table (revive: the first green gate run over 10
minutes), and then nothing remained undispositioned. The router lost its
brainstorm row, and its working-plan row stopped deferring to the
restructure plan by name — that plan closed 2026-08-04, so the row now
points at the snapshot record instead.

## Second pass: the timeline, and what it changed

After the owner interview (docs/scene-analyses/ chosen as the record's
home; delegation softened; crash.html promoted-after-review inside E; the
mechanical checks left filed), an independent no-context subagent
reconstructed the cold session's full timeline from the transcript. What
it added beyond the finding-pass, all now routed:

- **The missing field report has a mechanical cause**: the session's own
  six-task list mirrored SKILL.md steps 3–7 and omitted step 8, so when
  the last task closed nothing prompted the report — it was announced at
  delivery and never written. SKILL.md step 8 now says the report is a
  task if you keep a task list (0.19.6).
- **A correction to our own 0.19.4 wording**: instruments.md said the
  inert blink "shipped invisibly" — inherited from the builder
  postmortem's frontmatter drift. The transcript shows it caught at
  19:43:50, pre-delivery, incidentally. Fixed in 0.19.6; the
  derive-at-write-time lesson bit the analyzer this time.
- **The `| tail` habit is the QoL story**: it cost four recovery calls on
  the nocap sheet (including grepping build.js's own dispatch), hid
  strip's filename once, and cut motion's absolute-scale header. Filed as
  one output-hygiene row (operative line last, banner suppression,
  per-range strip names, probe refusing no-op expressions).
- **smoke is silent on parity when green** — verdict-states-scope,
  unapplied to the shipped smoke. Filed, revive at the next smoke edit or
  the E batch.
- Also recorded, no action: the 23k-token uninspectable design block, one
  composition round against a budget of 3–4, `aspect` never run, the
  exposure advisory absorbed three times, the export gate soft-skipped.

The record of all of it: `docs/scene-analyses/2026-08-04_market-crash-cold.md`
— a new document class (owner, 2026-08-05), registered in
source-of-truth's homes table and the router; the builder's postmortem
stays `(local)` evidence, the transcript-derived record wins on behavior.

## Evening: the E batch, and the owner's n=1 brake

The owner asked whether crash had moved to its official spot (it had not —
only the decision had landed), then approved the full E execution in an
interview: full batch, fix-then-promote, `scenes/` as the corpus name,
0.20.0. Executed: four films moved out of the shipped subtree, every
pointer/glob/workflow/hook moved with them (each named in the 0.20.0
changelog entry), `stage-films.sh` stages both homes (bracket green both
sides of the edit), and the smoke-parity-scope row's trigger fired with
the batch and was honored — full smoke now states its scope on green,
observed absent-then-present in a scratch workspace, bracket-parity's 33
arms green after.

The `film-reviewer` agent got its first real exercise, on crash: verdict
promote-after-fixing, with three real findings the build session's own
review never surfaced (the middle beats as three captions over one
continuous fall; the outro's uncovered loop seam; the reveal's
summed-ramps momentum stall — the documented anti-pattern, measured at
~0.35 candles/s across boundaries vs 3.0 mid-beat). All fixed in a
revised copy; instruments re-run green; the nocap sheet now shows the
three middle beats as visibly distinct stages of a drawn loop.

Mid-execution the owner applied the brake that matters: **do not advance
anything major on a single scene sample** — clarified as: more portfolio
test scenes get created AND reviewed before any "good enough, move on."
Routed: the fixed crash landed in `scenes/` (tracked corpus), NOT in the
shipped examples; the 2D teaching-baseline slot stays open with a
working-plan row (revive: a second 2D portfolio build reviewed); the
router's queue row now leads with building more scenes and says plainly
that VISION's cold-start criterion has one passing measurement, not a
verdict. The same-day promotion call and its same-day revision are both
recorded in `examples-placement.md`, because a decision that moved twice
in one day is exactly the kind of thing prose forgets.

## Filed at close: the vendoring question

Owner request at end of session: explore injecting three.js at build time
rather than tracking the ~1.1 MB embed per film in a public repo. Filed as
an Open question in working-plan with the day's measurement (scenes/ is
4.5 MB, ~4.4 MB of it four copies of the same IIFE; the 2D film is 43 KB),
the tracked-source-vs-delivered-artifact distinction that makes it
explorable without touching invariant 1, and the recorded
unvendoring warning it must engage. The router's queue row points at it.

## Session close

Postmortem of the day filed:
`docs/postmortems/2026-08-05_session_cold-mining-and-option-e.md` — the
distilled findings, including the one error of this session's own that
reached a release (the "shipped invisibly" attribution, caught by the
independent reconstruction, fixed in 0.19.6) and the forward item that
turns it into procedure: transcript-derived claims plus
/verify-written-claims on routing diffs, every time.

## Worth saying plainly

The cold session is the best single piece of evidence the project has that
the skill works as shipped: the docs alone carried a zero-context builder
through spec, build, three-axis review, export and delivery in one pass. The
two things it could not catch — an animation that never animated, and an
instrument silently narrowed by an authoring shortcut — are both now written
where the next builder reads, and both have their mechanical forms filed
with triggers rather than built in a hurry at the end of a mining session.

## Addendum, after close

Owner asked whether `.claude/settings.json` should be the local flavor. It
should not, and it already is not: the tracked file carries only the
claims-reminder hook wiring, written portably against `$CLAUDE_PROJECT_DIR`,
and that hook is a repo control with its own bracket — moving it local would
delete it for every checkout but this one. The personal half already lives in
`.claude/settings.local.json` (skill overrides).

The question found a real gap next to it, though: that local file was matched
only by a global gitignore outside the repo, so on any other machine or fresh
clone it would sit untracked and one `git add -A` from publishing per-machine
config. Added the pattern to the repo's own `.gitignore`, with the reasoning
inline so the asymmetry (one half tracked on purpose, the other ignored on
purpose) does not read as an accident later. `git check-ignore -v` confirms
the repo rule now matches; `.claude/settings.json` is still tracked;
`selfcheck.js` green.

No changelog entry: not plugin content (the cascade trigger check confirms
nothing under `plugin/` moved), and this repo's changelog is version-scoped
with 0.20.0 already published, so a repo-hygiene line has no released home.
Flagging rather than inventing an Unreleased section.
