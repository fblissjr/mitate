# 2026-08-08 — REP4 lands, and the reviewer learns what it never routed to

Owner opened with "let's continue" and, mid-session, a suspicion: the shipped
`film-reviewer.md` "is out of date and drifted — prevent it, or come up with
a better solution if it still serves a purpose." The queue's top item (the
cold build one tier down) stays blocked on a plugin-only session, so the
session took the next unblocked front: REP4, with the reviewer audit run in
parallel.

# First part — REP4 through the controls door (0.28.0)

The binding terms were already written: `representation.md` point 4 (derive
the base from construction, name every hand adjustment, bare numbers
forbidden — declare-and-compare explicitly rejected), and the gate in
`plan.md` (red on the corpus fixture before any shipped scene is touched).
Design followed from decided doctrine rather than fresh argument, which kept
the two real choices small: severity (warn, by `check`'s own rule — a bare
extent is satisfiable, and an ERROR would flip the whole corpus red at once,
the route-around shape `controls.md` warns about) and mechanism home
(`check`'s table reader — the SUBJECTS literal is already sliced, so the scan
is source-text only and stays off the prime directive's exception list).

**The mechanism.** A depth-tracked walk over the comment- and string-stripped
SUBJECTS literal; any numeric literal inside an `h`/`w`/`d` value expression
fires, one aggregated warn per scene naming every site. Derived bases
(`bear.height`, `walker.height`, `GRID_H`) pass; bare pads (`+.3`) fire —
the named-term half of the rule, which every shipped pad violated. The
standing footer now states the boundary honestly: FORM is checked here, the
VALUE still needs probe.

**Red-first, as the gate specified.** The warn named `walker (h: 6.1;
w: 2.8)` — row 11 — among the fixture's eight bare-extent entries before any
shipped scene changed. `templates/bracket-extents.js` covers each direction
the lint can rot: firing arms watched fail against the stashed pre-change
tree (3 of them, NO WARNING), quiet arms watched fail under a deliberately
over-firing mutant (FALSE WARN both), plus the corpus arm pinning the
fixture's signature. Adversarial variants beyond the fixture family
(yesterday's postmortem lesson): scientific notation and subtraction-pads
fire; near-name keys (`hd`, `wt`), template-string digits and `pos` literals
stay quiet; a quoted key and an entry-level indirection are disclosed edges
in the header; a fully minified declaration falls out of the table reader's
line-anchored scope and the verdict's coverage line says so.

**The first catch was the teaching template itself.** The base template's
knot declared `h:3.6`; the geometry's real bounding box is 4.788 tall
(computed with the workspace's three — a (2,3) torus knot's lobes swing half
again past the nominal radius), so every FS shot overflowed the frame by
about a quarter. Ninth independent instance of the ledger's
declared-extents-rot shape, this one shipped in the file that teaches the
form. The extent now derives from `computeBoundingBox()` in the build
function; the character template's `+.4` became `HEADROOM`. Both templates
pass `check` clean and the full smoke battery in a gate-style workspace; the
base template's pre-existing exposure advisory moved 44.7% → 47.0% with the
corrected (wider) framing — checked against the pre-change copy rather than
assumed pre-existing.

**Corpus migration queued, not done.** All seven 3D films draw the warn;
migration is per-scene value-preserving work (probe before and after, smoke
per film) and landed as a working-plan row paid on next touch. The fixture is
excluded permanently — its bare extents ARE row 11 and the bracket's red-first
subject. The first sweep said "all five" because its grep missed how
`gearbox` and `materials` spell the declaration; the claims pass caught it
and `check`'s own reader corrected the count to seven before the number
reached prose it couldn't be derived from.

# Second part — the reviewer audit: zero drift, real omissions

`doc-claim-auditor` ran twice at `plugin/agents/film-reviewer.md`. First
pass, every capability claim HOLDS — the file's design (defer instrument
internals to `instruments.md` and `smoke.js`) is what kept it from rotting
through five releases. The owner's follow-up asked the sharper question:
what's NOT in there, and what keeps it updated?

**Omissions, all real.** `build.js check` never mentioned as a command;
`probe` never named even though the file's own worked example ("beak tip at
x=1.07...") is probe-shaped output; the layer-attributed determinism verdict
and `--dump-frames` unsurfaced; dof on no axis; `breakdown.md` and
`characters.md` uncited while the flash-width default was restated inline
(a two-homes violation). All closed at 0.28.0: a "before any pixels" section
runs `check` first, a Geometry block gives probe the defect class no still
reaches, the Contract paragraph says a FAIL arrives pre-attributed, dof rides
the composition axis, and the flash default went home to `breakdown.md`.

**The control gap was the finding that generalizes.** Nothing content-checks
`plugin/agents/*`: selfcheck reaches only its links and its absence of dates,
`shipped-provenance.md` records that a pass happened without making one
happen, and `audit-claims`' routing named `.claude/agents/*` but not the
shipped directory — exactly the open question E4 filed. Settled both halves
in `audit-claims`' routing (`plugin/agents/*` on the agent bullet, both
READMEs on the capability bullet); E4 marked settled in the working plan. The
verdict worth keeping: the file sat in a gap between two documented systems —
covered enough by selfcheck to seem safe, named by neither routing list to be
safe.

# Third part — what the battery caught on the way out

`selfcheck`'s ratchet fired on the new bracket's own header (an arm count in
prose — reworded structurally). `bracket-selfcheck`'s pin-skew arm reported
MISSED: its mutation hardcoded `1.62.1` as the skewed version, which WAS a
skew until yesterday's bump made the pin equal to it — the arm was replacing
the pin with itself. Confirmed pre-existing against a clean-HEAD worktree
before touching anything, then fixed by deriving the skew (patch digit + 1)
so the fixture cannot go stale on any future bump; the arm now reports
CAUGHT and the static tier is green end to end.

The invalidation grep after REP4 landed found four live surfaces still
carrying the old framing — `plan.md`'s "still unbuilt" line,
`source-of-truth.md`'s unguarded-copy passage, the 2026-07-30 working-plan
section (annotated, not rewritten — it is a dated record), and the site's
"next on this track" — all moved to the landed state, the site in its own
voice with REP5 as the new next.

Closed with the cascade (0.28.0 across plugin.json, marketplace.json,
CHANGELOG), parity 12/12 carriers, all check-affected brackets green
(extents, commands, check-kit, corpus — row 11 probe still measuring 3.116
against the declared 2.8). On deck, unchanged: the cold build one tier down,
still waiting on a plugin-only session; behind it, REP5.
