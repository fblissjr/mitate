# 2026-08-07 — the batch audits itself: nine claims refuted, and the review agent gets its first look

The owner asked to go back over the recent major shifts, check they were done
the right way and actually verified, then continue. No new film yet — this
session is the verification pass, and it found enough to be worth its own
release.

## Getting up to speed, and one forward item that closed itself

The mechanical apparatus was green on arrival and stayed green: `selfcheck.js`
(22 checks), fence parity (10 files, 5,964 fenced lines against the canonical
store), `bracket-selfcheck` 30/30, working tree clean at `2efea51`.

The 2026-08-05 postmortem's forward item 3 turned out to be answerable rather
than pending. It asked whether the first push would exercise the bracket paths
0.21.0 repaired, and said plainly what each outcome would mean. The push landed
2026-08-06T00:53Z: gate run `31061108002` reports `every bracket ran (8), all
green` with every fixture resolving from `scenes/`, static `31061108001` green
beside it. Annotated on the postmortem as RESOLVED — section 2's repair claim
held under the only test that matters.

Worth noting how cheap that was. The item was written as a checkable
proposition with its own refutation condition, so closing it took one `gh run
view` rather than a judgment call.

## The verification pass

Ran `/verify-written-claims` over `c4f7990..HEAD` — the whole evening batch
(0.21.0, 0.22.0, 0.22.1, the site repair, the conventions commit, the
postmortem). Twelve claims held on re-derivation; nine did not.

The twelve that held are worth recording because they are the reason the
refutations are believable: five shipped brackets repointed in `401c1ed`;
check 2 deriving its scan set from `scenes/` (`selfcheck.js:34,95-101`);
`bracket-parity` at 33 arms; seven staged films; 14 shipped markdown files and
11 references; `crash.html` at 43,518 bytes; `plan.md`'s Examples policy
section carrying its SUPERSEDED banner correctly.

The nine that did not fall into two families, and the families are the finding:

**Half-migration.** `CLAUDE.md` contradicted itself about check 4 eight screens
apart — invariant 2 still described the provenance header that 0.22.0 inverted
away, while the conventions section described the inversion.
`source-of-truth.md` updated its homes table for the same move and left its
rules section asserting the opposite. `representation.md` counted five shipped
examples, of which there are now none, and survived because a
`<!--count-mention-->` marker exempts a line from the scanner without exempting
it from being false. A two-section document invites this: the edit lands where
the writer was looking.

**Same-day invalidation.** The three.js vendoring Open question was filed on
2026-08-05 with a measured basis (`scenes/` at 4.5 MB, five films, four copies
of the IIFE), and 0.21.0 moved `gearbox` into `scenes/` *later the same
evening*. Re-derived: 5,792,858 bytes across six films, 5,749,340 of it five 3D
films. Worse than a stale number — the question's *hardest* design constraint,
"the shipped example must reach installed users runnable with no toolchain,
`plugin install` is a copy, no build step exists to inject at", stopped existing
in that same commit. The question is materially easier than it was written to
be, and nobody would have known without re-reading it. This is exactly the
shape `/verify-written-claims` step 5 exists for, and it is the third time the
repo has caught it.

Also: the changelog promoted a mid-scrub reading of "26 dated lines" to a
before-and-after figure — the pre-scrub tree carried **40**, across 12 files. A
backlog item still asked for a line in a README deleted at 0.21.0. And the
site's fact tile claimed "three.js embedded" per film on a page that cards a
Canvas2D film and says so two hundred lines above.

## The review agent, verified for the first time

Two owner calls came out of the report. On the ledger: add the missing row, and
verify the file before writing it. On the site: fix the fact tile only, leave
the title and meta as positioning.

`plugin/agents/film-reviewer.md` had shipped since the plugin had an agent and
had never been audited end to end. It was also the one shipped markdown file
with no row in `docs/shipped-provenance.md`, and the reason generalises: the
0.22.0 scrub's working set was "files carrying a date", and this file carried
none, so it fell out of the batch *and* out of the ledger the batch created.
**A scrub whose file set is what the check matches is narrower than the rule
the check enforces, and the gap is invisible precisely because the check is
green.**

Its first verification found three things:

- **A false mechanism claim.** The brief stated that "both the 0.6 and 0.95
  samples land inside `CONFIG.flashes` windows on the beats bracketing a world
  cut" as a structural property. It is arithmetic dependent on beat length and
  flash half-width. `scenes/crash.html` is the only scene in the corpus that
  declares flashes (`{beat:'halt', at:0, w:.12}`), and the preceding beat's
  `0.95` sample lands 0.175s before the anchor — outside the window. The claim
  is false on the only case that exists. Now stated as the arithmetic to
  compute, with the real common case named.
- **Three reference pointers that dangle from an install cache**, written
  `plugin/skills/mitate/references/...` — which resolves from a clone and from
  nothing else, since the plugin subtree *is* the cache root. Same invariant-3
  class as 0.22.1, and invisible to selfcheck check 5, which resolves markdown
  links rather than backticked paths. The same file used the correct bare form
  two lines below, so it was internally inconsistent.
- **Three amendment narratives that outlived the 0.22.0 scrub** ("this cited a
  repo-local rules file until it shipped", "this line dropped both for four
  versions", "wider than this line once claimed"). The rule is no dates, no
  amendment notes, no decision narratives; check 4 matches ISO dates, so it
  enforces one third of it. Removed.

Landed as **0.22.2** (cascade complete). The ledger now states its row set as a
rule that can be run — one row per line of `git ls-files 'plugin/' | grep
'\.md$'` — rather than a list to be trusted. Verified: 14 files, 14 rows.

## The bracket artifact, filed on its third sighting

`bracket-selfcheck.js`'s cascade arm reported MISSED during the pre-commit run.
It is the documented pending-bump artifact: check 11 diffs against the last
commit touching `plugin.json`, so an uncommitted bump already satisfies it and
the injected violation has nothing to prove. Committed, re-ran, arm went to
CAUGHT — all 4 repo-side brackets green (12+4+30+3 arms).

Filed rather than diagnosed a third time. It is disclosed in the arm's own
comment, but the disclosure lives in the source while the surface prints *"Do
not trust it until this is 0"*, and a cascade commit is exactly when someone
runs it. Observed 2026-08-02, mid-batch 2026-08-05, and today. The fix shape —
detect the pending-bump state and SKIP with the reason stated — is a change to
what a control reports, so it goes through `controls.md`'s door. Row is in
`working-plan.md`'s deferred table with its trigger.

**FIXED later the same day** — see the second half below. It surfaced a fourth
time during a cascade in the afternoon, which was one of its two triggers, and
the other (an edit to `bracket-selfcheck.js` for any reason) had fired in the
same breath. Built as filed.

## Not run, and saying so

The browser gate's brackets (`templates/bracket-*.js`) were not run locally.
Nothing this session touched is scene content — prose, one site line, one agent
file — and CI runs them on push. That is a scope statement, not a green.

## Two owner corrections, mid-session

**Unmeasurable success criteria are deleted, not filed as gaps** (0.22.3).
"Whether a beat is funny, warm, or tense" left `instruments.md`'s ledger. The
framing was the defect: a list titled *what has no instrument* turns each
entry into work someone should do, and an undecidable property has no ground
truth to build an instrument against — so that work cannot fail, which means
it cannot succeed. It also quietly relocates what a corpus film is for. This
bit me directly: I had pitched the film as valuable *because* its success axis
was unmeasurable, which is backwards.

**A film's build class is part of its evidence** (`1afd775`). A film built in
this repo and one built from the installed plugin answer different questions,
and only `crash` said which it was. `scenes/README.md` now defines COLD and
WARM and labels all seven, with the asymmetry stated: a cold build's
smoothness is evidence the docs work; **a warm build's smoothness is evidence
about nothing**, because the builder read the source.

## strider-intro — Rung A of boss-intro, built WARM

`plan.md` already specified the rung below `boss-intro`: title card and
dramatic lighting on an EXISTING creature, separating register work from
creature invention. So the strider is `menagerie`'s proportion vector
verbatim and the register is the only new variable. Seven beats, 20.4s.

Chosen for what it would *exercise*, all three verified unexercised across
the corpus first: `STYLE.dof` + a rack focus, `match: true` (zero occurrences
repo-wide), and per-shot camera energy (which does not exist — hitting the
wall was the point).

**The headline: enabling `STYLE.dof` proved it had never worked.** Landed as
0.23.0 with `bracket-dof.js`. Detail in the changelog; the part worth
repeating here is the shape. It was not inert — it applied a uniform
imperceptible softening — so "is there an effect" passes in both the broken
and fixed states and cannot discriminate. Only "does the parameter do
anything" can, which is why the bracket is a parameter sweep. And the solver
half was correct the entire time (`shotFocus` 8.18 → 5.62, probe-measured),
so a reader checking the documented mechanism would have found it working.

Review, three axes: composition converged in three rounds plus a fourth after
dof went live; `motion` 0 dead-air stretches (down from one 1.25s stretch);
semantics reads with every word stripped; foot planting confirmed by strip.
The shard's composed periodic was probe-checked at 5 distinct values rather
than assumed — the one instrument gap I wrote into the code on purpose.

Two composition findings worth keeping: the `rhyme` beat first framed a black
box, because I built the husk as a shell of radius 1.5 around a core of 1.15
and it enclosed the light entirely — the payoff beat of the whole match cut,
caught by the contact sheet. And the creature was an unlit blob at every rung
tighter than FSA, which **the exposure advisory had already diagnosed in
words** ("add a fill/rim light") while I was ready to dismiss it as expected
for the register. That is the "absorbed three times" pattern with me in the
chair.

### Field report (SKILL.md step 8)

- **Built twice:** nothing. The one thing I reached for twice was the
  beat-addressed two-beat span — once for travel, once for the gait envelope
  — and the second was a *consequence* of the first, not a rebuild.
- **Re-derived from scratch:** the low-key lighting rig. There is no lighting
  reference; `bibles.md` owns "the look" as a swappable object but carries no
  key/fill/rim method, so the ratios, the hemisphere floor that keeps a dark
  film off the hard-fail branch, and the rim-vs-camera-azimuth relationship
  were all worked out here from nothing. **This is the strongest candidate
  for the next harvest** and I am deliberately not writing it into a
  reference off one scene.
- **Copied out of an earlier scene:** `menagerie`'s strider proportion vector
  verbatim, on purpose — Rung A isolates the register, and inventing a
  creature at the same time would make any failure unattributable.

### Snippet harvest

Paid two, both lifted from instrument-green code rather than authored fresh:

- `characters.md` gains the **multi-beat gait envelope** — `ramp`/`pulse`
  address one beat, so a walk spanning two needs travel and envelope written
  against the same pair of edges. The failure is silent and severe: widen the
  travel, leave the envelope, and the figure skates in on locked limbs with
  smoke green and no strip pop. I walked into it and the strip caught it.
- `film-language.md` gains the **worked rack focus** with the sizing rule —
  probe `shotFocus` at both ends and pick a `focalLength` of the same order
  as the pull, since at 40 the rack is invisible while every check passes.

Not paid: the three older debts (instanced-field outline pass, `normalBias`
shadow fix, physical/SSS demo). None of them appears in this scene, and the
rule is that a snippet is lifted from a passing scene or it stays filed.

## Also observed, filed not fixed

- **One flaky poster capture in ~8 renders.** `menagerie` rendered
  `a7fd5a92` once at t=6.0 and `703dbfab` on every other run, before and
  after the fence change, three runs each side. Same class as the capture
  race 0.16.28 addressed in `seekSynced`; whether `build.js poster` shares
  that guard is not established here. Recorded with conditions, not
  diagnosed.
- **`bracket-selfcheck`'s cascade arm reported MISSED a fourth time**, which
  is the trigger the row I filed this morning names. Still filed, now with
  its trigger fired.

## The 2D rung turned out to be the wrong shape, and saying so was the work

Started on it, then stopped. Its stated reason is the n=1 brake, the brake is
on the **cold-start** criterion, and a second 2D film built by me is WARM — so
it would have moved the queue without moving the measurement, which is worse
than not doing it, because a discharged-looking row stops anyone else running
the real experiment.

The warm bug-hunting case was checked rather than assumed, and it was weak:
parity DOES guard the 2D kernel (inject a line inside crash's `KERNEL` and
`--parity-only` goes red), smoke's checks are not backend-gated so
determinism, framing invariance and live playback all run on 2D, and `aspect`
holds the same composition across all four window shapes on `crash`. No
STYLE.dof-shaped defect was sitting there. Requeued as blocked-on-cold.

**A correction I owed on the way**: the day's earlier claim that `crash.html`
"carries no fence markers at all" and sits "outside the parity set" was
**false, and it had already been pushed**. The check behind it grepped
`mitate:begin:X`, a marker syntax this repo does not use; the real markers are
`==== X-START ====`. An empty result was read as an absence rather than as a
query that never had a chance to match. Corrected in place in `plan.md` with
the general form attached, because it is the one worth keeping: **a query that
returns nothing is a claim about the query until you make it return
something.**

## hauler — Rung B, and the bug it took out of its predecessor

Creature invention from text, quadruped, opposite corner of proportion space.
Landed as 0.24.0. The headline is not the creature:

**`SUBJECTS[*].pos` reading `getWorldPosition()` is not pure.** `seekTo` runs
`setCamera(state); animate(t); …` and `setCamera` is what calls `pos()`, so
the matrix it reads was written by the PREVIOUS frame. The subject is a
function of (t, previous t). `hauler` failed smoke at `t=16.64` immediately.
**`strider-intro` had the identical pattern, shipped, and passed** — no
sampled `t` landed where the stale pose moved a byte. Both fixed; the rule and
both spellings are now in `film-language.md`.

That is the second time today the same lesson arrived in a new costume: a
green check certifies what it sampled, not the property it is named for.
STYLE.dof was "is there an effect" passing in both states; this was a
determinism sampler passing over a genuinely impure scene.

### Field report (SKILL.md step 8)

- **Built twice:** the low-key lighting rig — deliberately, and that was the
  point. It was re-derived rather than copied so the second application would
  be evidence about the method instead of an echo. What differed between the
  two is now the content of the `materials.md` section; what stayed the same
  is the four roles.
- **Re-derived from scratch:** the multi-beat gait envelope, in a new form
  (two disjoint travel legs rather than one span crossing a boundary). The
  rule I wrote into `characters.md` this morning covers the one-span case and
  I had to extend it in my head for the two-leg case. Worth watching whether a
  third film needs the generalised statement.
- **Copied out of an earlier scene:** `strider-intro`'s shot grammar almost
  wholesale — rack focus, whip into an insert, match cut out of it. Held
  constant on purpose so the creature was the only new variable. Two things
  would not copy: every rung on the body had to go WIDE (a wider-than-tall
  subject has no waist, so MS/MCU/CU carry meanings it cannot satisfy), and
  the rack had to pull the other way.

### Snippet harvest

- `materials.md` gains the **low-key rig**, released by this being the second
  scene to use it. The three things that MOVED between the films are the
  useful part, not the four lights.
- `film-language.md` gains the **`pos` purity rule** with both spellings and
  the sampling caveat.
- Still unpaid, correctly: the three older debts. None appears in either film.

## Also observed, filed not fixed

- ~~**A static check for impure `pos`** — filed with its red arm named.~~
  **BUILT, same day** (`selfcheck.js` check 23, two arms in
  `bracket-selfcheck.js`). Red on real impure code, green on the fixed
  corpus, both observed unpiped. Its first draft fired on the two scenes that
  had just been fixed, because their comments *name* `getWorldPosition` while
  warning against it — the check reported its own documentation as the
  defect. Comments stripped before the test; the second arm pins that a
  comment-only mention must stay green. The 0.24.0 changelog line saying
  prose was the only guard was true when written and false within the hour,
  and is corrected in place.
- **`motion` cannot see a focus pull.** The `cradle` beat scored 0.09 and
  reported 2.0s of dead air over a window where the rack focus was running.
  True dead air as well, and fixed — but the instrument's blindness to a
  deliberate optical change is its own note.
- Carried over: the `bracket-selfcheck` cascade artifact, and the flaky
  poster capture.

---

# Second half — three cold builds come back, and the analysis becomes an instrument

The owner had run three film builds in isolated workspaces overnight and
brought back the scenes, the field reports, the postmortems and the
transcripts. Two are a matched pair on an identical brief — one in an empty
workspace, one in the workspace that had built the market-crash film — and the
third is a video-to-scene attempt. The ask was to learn how an agent uses the
plugin in practice, and to make that a standing feedback loop rather than a
thing the owner has to specify each time.

## The comparison the repo had planned for is unavailable, and saying so first

Three independent no-context subagents reconstructed one build each from its
transcript. Before dispatching them I should have queried model and effort and
did not — the owner had to ask. Queried from the transcripts `(local)`,
2026-08-07: the 2026-08-04 baseline ran **sonnet-5** with no effort field in
its log at all; today's three ran **opus-5** at xhigh, xhigh and high.

That kills the comparison `docs/README.md` had set up. Six axes moved at once —
model, effort, plugin version (0.19.3 → 0.22.1), input modality (text only → a
source image and a source video), duration (37s → 90s), and activation (routed
on the description → explicit slash command). Reference reads went from 2-of-11
to 5-to-8-of-11, which is real and **cannot be credited to the doc work**: a
bigger model at higher effort reading more of everything predicts the same
result. That is now written into the routing row so a future session does not
quietly take the flattering reading.

What the set does supply is one controlled experiment — the turtle pair, same
everything but the workspace.

## What three independent builds hit the same way

Recurrence is the promotion bar, and these cleared it:

- **The scaffold installed outside the workspace, 3 of 3.** `bun add` with no
  local manifest walks UP and installs into the first one it finds. Two
  detected it and spent roughly ten and six minutes recovering, one of them
  editing a manifest and lockfile outside its own workspace; the third never
  noticed and its workspace still has none. `| tail` was not the root cause —
  bun prints `installed three@0.185.1` with no destination either way.
- **`webgpu-stack.md` opened by none of them, during determinism failures in
  all of them.** Roughly 53 minutes across the three rediscovering what that
  file documents. `delivery.md` and `recordings.md` also unopened by all three,
  and all three exported MP4s.
- **All three patched a private copy of `smoke.js`** to see *what* a failing
  determinism arm compared. Three independent reinventions of one missing flag
  on one day.
- **Every builder overstated its own debugging effort**, three of them by six
  to sevenfold. That is the argument for reading transcripts instead of field
  reports, and it is now doctrine.

## The contamination answer, and it was not the expected channel

The warm build had **no prior memory** — no `CLAUDE.md`, no `.claude/`; it
created the memory directory itself near the end. Its code is clean and
verifiably so: no read of the prior scene anywhere in its tool inputs, zero
identifier overlap, different template, fence parity held throughout.

What it took was the **prior session's postmortem**, read in full at minute
nine, unprompted. The owner's mid-session instruction said "code or patterns";
the reply narrowed it to one filename, and the postmortem already in context
was never mentioned or withdrawn. It then executed two of that postmortem's
forward items as work, and mined its HTML for figure CSS a hundred minutes
after being told not to lift patterns. Neither of the build's own deliverables
mentions the intervention happened.

The durable finding: an isolation instruction that names *files* does not reach
what is already *read*.

## 0.24.1 and 0.24.2 — the scaffold, and the fix's own version of the bug

0.24.1 put a scene-local `package.json` in SKILL.md's scaffold, fixed the same
shape in `gate.yml` (CI had been exercising the bug rather than the prescribed
path, so it could never have caught this), and fixed the scaffold block
contradicting the template table two lines above it. Verified red-first in a
scratch tree, then the whole fixed block run end to end.

Reviewing that, 0.24.2: the new line used a bare `>`, which truncates an
existing manifest — the same class of silent damage in the other direction.
Guarded with `[ -f ]` in SKILL.md and both workflows.

## The drift question found two live holes

Asked how the new analysis skill avoids going stale, and the question answered
itself twice.

**`selfcheck.js` check 7 derives which docs carry a freshness marker by looking
in a 200-char window** — and a skill's frontmatter `description`, long by
necessity, pushes the marker past it. Two repo skills carried markers nothing
had ever checked; two had never been given one. Silent by construction: a file
outside the population cannot fail. Demonstrated by setting `extract-patterns`
to 2020-01-01 against a 2026-08-05 commit and watching selfcheck stay green.
The window now skips frontmatter, and the first thing it caught was real —
`extract-patterns` had been stale for a week.

Then the harder half: stripping frontmatter fixed the *case* and left the
*class*. A marker below the window still vanished silently. A marker that
exists outside the window is now a failure naming the file.

**`sample.yml` was a fifth consumer of the playwright pin and the only one
nothing compared.** Now checked against `gate.yml`, and its note names any pin
it could not compare rather than reporting a bare agreement count.

All three new checks are bracketed, and each arm was verified to go MISSED with
its fix reverted — not merely CAUGHT with it present.

## The cascade artifact, fixed on its fourth sighting

Both triggers fired at once. Arms now take an optional precondition; the
cascade arm detects a pending bump and SKIPS with the reason printed, and the
runner reports exercised-and-skipped beside its derived total. Verified the
skip is conditional rather than permanent — the arm returns to CAUGHT on a
clean tree — because a precondition that always fired would silently disable
the arm, which is worse than the MISSED it replaced.

## The instrument, and the retro that specced it

`.claude/skills/analyze-build-session/` — the second flywheel's tool, and
`VISION.md` now carries the argument beside the pattern flywheel it already
had. It was specced from the retro on this pass rather than from the morning's
hypothesis. The hypothesis got the extraction schema and the never-opened
complement right. It missed: the orchestrator/subagent split, five query traps
(worst: tool results are `user`-type messages, so a naive human-turn count is
~5x high), verify-against-the-current-tree, verify-against-the-code (which
caught a deliberate warning one step from being written up as a false
positive), and recurrence as the promotion bar.

The honest finding about this session's own work: **the first run wrote
subagent-derived claims into a tracked record having spot-checked only the two
that became actionable.** The record now states its evidence tiers instead of
implying one, and the skill makes verify-against-the-subagent the first of four
passes.

Anti-drift design, since no CI check can reach it: repo claims are covered by
`/audit-claims` and now by check 7; schema claims fail loud; **semantic** claims
are the dangerous class and cannot be covered at all — the fixtures are
local-only and a bracket that cannot run from a clean checkout is not a
bracket. So each ships with the one-line command that falsifies it, run per
fixture.

## Not run, and saying so

- **The `playwright-core` bump is held, deliberately.** 1.62.1 ships Chromium
  151.0.7922.34 against 1.61.1's 149.0.7827.55 (read from each package's own
  `browsers.json`, 2026-08-07) — two Chromium majors, so a different ANGLE and
  Skia, which invariant 5 makes a backend change in all but name. Bumping now
  would invalidate every determinism baseline at the moment they are the
  measuring instrument. Owner agreed the sequence: build the readback
  discriminator as a real bracket on 1.61.1, characterise a baseline, then
  bump. `three` needs nothing — 0.185.1 is already the latest published.
- **The determinism lead is a lead, not a diagnosis.** Three sub-perceptual
  shadow-adjacent reproductions landed in scenes nobody built for the purpose,
  and one build's discriminator measured *screenshot differs 6/8, canvas
  readback 0/8* — pointing at the observation layer rather than the scenes.
  None of it was collected as a control, so it is filed as starting evidence
  against the open 1-in-6 metal FAIL and nothing in `plan.md` moves yet.
- The home-directory manifest that had been absorbing the escaped installs was
  cleaned, after giving five local fixtures their own installs first — three of
  them had been resolving `three` from it, including the only candidate
  reproducer for the determinism lead.

## What is on deck

**The 2D rung, cold** — it needs a plugin-only session, which is the whole
point of it. Then whatever the corpus says next. The impure-`pos` check is the
first thing to build if a third film reaches for a rig part.

Ahead of both, from the second half: the **readback discriminator as a real
bracket**, which gates the playwright bump and is the first step on the
determinism lead. And `smoke.js --dump-frames`, which three independent builds
asked for by building it themselves.

---

# Third part — the harvest queue disposed, as its own session

The next session took the work-next row at its word: front (1), the nine-row
harvest queue, disposed end to end. Landed as **0.25.0** (cascade complete).

**Verification before writing, and it earned its keep in a small way.** Every
row's grep was re-derived against the current references before anything was
written. All nine held — but one *appeared* to fail first: `proud` returned
zero hits in `materials.md`, directly contradicting the queue's own
verification note, which would have been the second failed sample and the
extraction-precision alarm the postmortem named as the wrong-premise
condition. It was my grep — the file says `PROUD`, uppercase, and the query
was case-sensitive. A query that returns nothing is a claim about the query;
the day already wrote that rule down once, and it fired on the person
citing it within hours.

**Content re-derived from the fixtures, not the summaries, for the rows that
could bear it.** The contact disc was lifted from the restage's
`contactShadow()` (delivery-green scene); the pool-ceiling fix was read out of
both turtle scenes' code, which independently agree (strided access
`R[(i*k)%R.length]`, plus one written-out closed-form hash); the three
false-green shapes came from the warm build's own postmortem `(local)`, whose
structural line — the control held its independent variable constant and read
the resulting constancy as agreement — went into `method.md` nearly verbatim,
because it is better than any paraphrase. The facing × angle fact was derived
from the SOLVER fence's own placement arithmetic (`angle:0` on +Z, `90` on
+X), not from anyone's account of it, and landed in `film-language.md` per the
composed-fact decision, with `breakdown.md` pointing.

**Declined, deliberately, and recorded as such:** the two fence-priced
helpers (contact-disc kit primitive, kernel cross-beat ramp), each at two
instances — the queue's own pricing says the paragraph ships first and a
third film pays for the helper. No fence was touched all session, which keeps
the change prose-only and the parity set unmoved.

**The determinism lead was kept out of the shipped prose on purpose.** The
contact-disc paragraph's honest advantage — ordinary geometry takes the shadow
pipeline off the determinism surface — is stated as capability; the restage's
shadows-on/shadows-off measurements were NOT written into the reference,
because the repo has that filed as a lead, not a diagnosis, and a reference
asserting the scene-layer cause would front-run the readback discriminator
that exists to settle it.

Ledger increments applied in `pattern-ledger.md` rather than left filed:
built non-DOM text 1 → 3 (the type-primitive trigger has now fired, twice
over, on the same shipped constraint), declared-extents-rot 6 → 8. The
work-next row now leads with the readback discriminator, then
`--dump-frames`.

# Fourth part — the determinism verdict learns to name its layer (0.26.0)

The owner pushed 0.25.0 and said continue, so the session took the next two
fronts, which turned out to be one front: the readback discriminator,
`--dump-frames`, and the situated pointer all live in the same two failure
paths in `smoke.js`, and the misleading "Usual cause" sentence dies in the
same edit. Through `controls.md`'s door, red-first at every step:

- **Baseline first.** `bracket-driver` 18/18 and `bracket-determinism` 3/3
  against the unmodified tree, from the crash workspace.
- **The new bracket before the change.** `templates/bracket-readback.js` was
  written first and run against the unmodified `smoke.js`: all four behavior
  arms failed, each with its own marker, and the capture arm's
  FORBIDDEN-MESSAGE-PRESENT documented the exact misattribution being closed
  (a stable canvas blamed as scene state). The wall-clock DOM fixture
  reproduces screenshot-differs/readback-agrees from a clean checkout
  deterministically — better than the N/8 the forward item asked for, with
  the honest caveat written into the bracket header: it proves the instrument
  discriminates, it does not retro-diagnose the field failures.
- **The change, with anchors preserved.** The driver's two anchors were each
  observed failing against the new smoke before being updated — and the
  second failure was itself informative: the disconnected-push arm's
  destructure anchor no-opped, leaving a half-applied mutant whose throw was
  being read as a verdict.
- **After:** readback 5/5, driver 18/18, determinism 3/3, full corpus smoke
  green (known exposure advisories only).

**The baseline the bump was gated on now exists.** 24 metal runs of
`bear-and-bees` on `playwright-core@1.61.1`, 24/24 `all scenes pass`,
verified per file. A standing 1-in-6 rate survives that sample ~1.3% of the
time. Stated carefully everywhere it is written: this does not retire the
lead — the flake did not reproduce, so the discriminator has never seen it
live — it makes the bump an experiment instead of a confound. The bump is
now the owner's call.

**The session ate its own dog food twice.** The first characterization
attempt piped the validator through grep and read silence (the
check-exit-codes rule); the second failed on a stale `cd` leaving
`NODE_PATH` pointing at nothing — the exact wrong-artifact shape written
into `method.md` an hour earlier. Both caught by their exit codes and
re-run pinned.

# Fifth part — the review lands, the bump executes, the snapshots leave (0.27.0)

The owner pushed 0.26.0, launched `/code-review` on it, and directed three
things at once: bump playwright, archive the snapshots, and take a last look
at `.claude/` and `.github/` before closing the session.

**The review earned its run.** Two confirmed findings against the
discriminator, and the first was the discriminator's own failure mode
inverted: a GL canvas without `preserveDrawingBuffer` reads back fully
transparent — byte-stable zeros both sides — so a genuinely nondeterministic
scene drew the capture-layer verdict and was told it was innocent. The
reviewer reproduced it against a live page before reporting. Fixed with a
labelled `readback-blank` substitution, and the new bracket arm was watched
drawing the innocence verdict against the pre-fix tree (stash, red, pop,
green — 6/6). The second finding was the unavailable branch printing a
healthy hash as the reason attribution did not run; now both hashes print.

**The bump ran as the experiment the hold was designed for.** 1.62.1 /
Chromium 151 installed through the pinned package's own CLI (found on the
way: `bunx playwright` fetches the latest full playwright, whose Chromium can
differ from the pin — SKILL.md and sample.yml now route through
`node_modules/playwright-core/cli.js`). Identical battery, identical result:
corpus green, readback/driver/determinism brackets green, 24/24 metal runs on
`bear-and-bees` — same count as the 1.61.1 baseline taken hours earlier.
Nothing moved that the baseline can see. Every pin consumer updated; the
hold rule scrubbed from the live surfaces per the owner's direction, with
the why kept as history in the lead row.

**Snapshots archived out of the tree (owner call).** Untracked, gitignored,
moved to the local archive. The ripples were the real work: the CLAUDE.md
Map bullet removed (the Map's completeness claim is over tracked entries),
`source-of-truth.md`'s dated-record class no longer includes them, every
live pointer at a snapshot record now says archived, the memory index
updated, and the derived-counts-vs-snapshots classification conflict in the
deferred table dissolved — archival removed its subject. One near-trap: the
owner's message said the gitignore edit existed, and the working tree said
it did not; added here rather than assumed present.

**The `.claude`/`.github` last look was quiet.** No stale pins, no snapshot
paths, no invalidated instrument claims; `static.yml` carries no playwright
pin, and the new bracket rides gate.yml's glob without an edit. The
selfcheck ratchet fired once more on a "verified" in the new arm's comment
— reworded to name the arm as its own standing control.
