# 2026-08-04 — 0.18.2: three silent substitutions learn to speak

Picked up exactly where the handoff pointed: the REP2 review's three
behavioral findings, filed in working-plan's 2026-08-03 section, shared
root already named there — silent input substitution. All three closed in
one cascade, red-first in the literal order every time: arm written,
watched red against the pre-fix verb, fix, watched green. Baselines first
(both brackets green before any edit, from the circus workspace).

1. **Anchor fraction from a scene constant.** `at:['title', FRAC]` was a
   false ERROR quoting `undefined` — the 0.16.70 class one field over, as
   the queue predicted. New `bracket-commands.js` arm (`fracconst`,
   deliberately beside the `durconst` arm it mirrors) went red on the
   false ERROR, then green on the warn. The KEYS spelling (`{beat, at}`)
   got the same exemption in the same edit — fixing SHOTS and leaving
   KEYS would have been the one-field-over pattern repeating itself.
2. **Dot-assignment.** `mutatedAfterDeclaration` knew `.push()` and
   bracket-assignment; `SUBJECTS.legend = {...}` slipped past, so check
   read the stale literal while claiming coverage and errored on a scene
   that drives. Before widening the regex, the corpus was grepped for the
   new spelling across all three carrier directories — zero hits, so the
   change condemns nothing shipped. Arm red (false `unknown subject`
   ERROR), then green (declared assembled-at-runtime warn, stub proxy,
   exit 0).
3. **The STYLE stand-in declares itself.** The largest one: a bible
   STYLE is beyond the literal reader, so the solver ran on a quiet `{}`
   whose lens default (42) passed a match cut the scene's real lens (35
   in the fixture) makes the page refuse at boot. The sixth
   `bracket-check-kit.js` arm needed a new arm FORM — phrase as
   `{check, driven}`: check must exit 0 SAYING the stand-in while the
   driven page refuses with the match-cut throw. Red run showed exactly
   the divergence (check green and silent, page exit 1), green run shows
   the declaration. Checked before building: gearbox is the one shipped
   scene with a bible STYLE and its shots carry no `match`/`fov`, so the
   warn is quiet on the whole corpus — no forever-warn on a correct film.

Also in the cascade: `execKit`'s `k in t` → `Object.hasOwn` (the review's
diff-pass nit — prototype-chain lookup, same shape as the KERNEL's
null-prototype BEAT fix one layer down); `instruments.md`'s check section
corrected (SIZES-from-store, the executor's environment seam, the
declared-substitution behaviors, and a rider distinguishing bare
references from arithmetic on them); the build.js strictness list marked
as a class not a list (the `38e3773` correction finally applied to the
file that still had the old shape); bracket-check-kit's header identity
claim scoped to inputs check can actually read.

Three incidents worth the narration:

- **An arm caught its own subject's prose within minutes.** The fraction
  warn's first wording dropped the `cannot resolve` phrase the arm pins
  (and the dur warn carries). Green-run FAIL: exit 0, stdout lacking the
  phrase. Wording realigned with the dur sibling. The control doing its
  job against the person who wrote both sides of it, same hour.
- **The provenance addition broke a check two files away.**
  instruments.md's new dated line pushed the "Not here" edge past
  selfcheck's 2500-byte header window (the committed file had 70 bytes of
  headroom). First attempt compressed the new line — still 188 over. The
  durable fix moved the edge up, directly after the dated-verification
  paragraph, where future provenance lines cannot push it out. Window
  untouched: widening the measure to fit the prose is the move invariant
  6 exists to block.
- **The claims reminder earned its keep on the CHANGELOG entry.** The
  derivation pass it demanded found one claim written before it was true:
  "selfcheck green" was in the entry while selfcheck was still red on the
  version-cascade check (the bump had not landed yet). It became true two
  minutes later — which is exactly the class: prose describing the
  intended state as the current one. Re-ran, then kept the sentence.

Post-landing invalidation grep (the reminder's other demand): two hits.
docs/README's work-next row still called this "the 0.18.1 red-first
cascade" — the cascade landed as 0.18.2, 0.18.1 having gone to the corpus
bracket the night before — and the site's REP card claimed the standing
control "drives both against every divergence case review has found so
far", which the two new check-side arms (not driven-both) and the
declared-divergence arm (not agreement) turned into an overclaim. Both
corrected; the site now says what the sixth arm actually pins — honesty
where agreement is unachievable.

Verification at landing (all this machine, fallback backend, 2026-08-04):
bracket-commands 38/38, bracket-check-kit 6/6, parity 9 files / 5,796
held lines, `check` clean over all 8 scenes (5 examples + 3 templates, 0
errors 0 warnings each), selfcheck fully green after the cascade.

## Afternoon: the tableValue disposition, REP3 opened, and an orientation postmortem

The owner asked where the tableValue conversion should go. Walked the
options against the record rather than taste, and the record answered:
sandbox-evaluating the scene's prefix violates representation decision
point 1 (data is what is decidable WITHOUT running the scene — executing
the film to validate its data inverts the boundary); exporting tables via
the contract violates VISION twice (the contract's thinness, and
validation before a frame renders); a vendored parser is declined by
execute-don't-mirror (a better mirror is still a second model of JS) and
by the measure-first habit, priceable if the parser-fidelity class
recurs. Disposition agreed: expand the mirrors-table row, revive trigger
"a false verdict from this reader reaches a shipped film" — plan.md's own
"blocked or reliably wrong" tier applied to this mirror. Row edit queued
behind the postmortem below.

REP3 opened with its prerequisites read in full (VISION.md,
representation.md's decision, breakdown.md), and one measurement taken
before designing: the fence store's own reads, enumerated mechanically
(`grep -ohE 'STYLE\.[A-Za-z_$][A-Za-z0-9_$]*' fences/*.fence.txt`, same
for CONFIG). The store reads 7 STYLE keys (bg, bloom, cutDur, dof,
energy, exposure, lens) and 8 CONFIG keys (cameraFloor, capFade, flashes,
flashWidth, seed, subtitle, sway, title). breakdown.md's enumeration has
the kit reading 12 STYLE keys, so 5 (ink, stroke, accents, fontFamily,
titleInk) are read by template code OUTSIDE the fences — which means a
registry derived from the store alone under-covers, and the derivation-
source question (store only, or store plus templates, and what a
workspace without templates does) is the first REP3 design decision.

Then the owner asked for orientation — what the two days built, how plan
and working-plan relate, whether they are maintained — and a span
postmortem was filed:
`docs/postmortems/2026-08-04_span_project-orientation.md` (with an HTML
render beside it). Its evidence pass found the day's own escape: the
morning's invalidation grep after 0.18.2 covered docs/README, the site,
root README and CLAUDE.md but not docs/plan.md, whose REP2/REP3 stamps
still said "the 0.18.1 cascade" at lines 1048 and 1060. Fixed in the
same commit as the postmortem. The transferable form: grep for the moved
token itself across all tracked prose, not a remembered list of status
surfaces.

## REP3 lands as 0.19.0 — the open bags police their own keys

The derivation-source question answered itself once the five non-store
STYLE keys were located: all five are read only by scene2d's drawing
scaffold, which a film inherits into its own source — so consumption is
decidable from the scene's own text (a carrier embeds its fences), and
the store scan is needed only as the near-miss vocabulary. No template
lookup, no environment variance, no annotation.

Second measurement before building: no carrier consumes STYLE/CONFIG
indirectly (destructuring, Object.keys, computed access — keyscan over
all 8 scenes plus after-hours), so plan.md's film-private MARKER was not
built; the deviation is recorded in the REP3 stamp with its trigger, and
the gap is self-announcing (indirect consumption draws false warns on
exactly the keys it consumes).

Red-first: the bracket-commands pair went in first (misspelled `exposur`
must fire naming `exposure`; a declared key the scene reads must stay
quiet), firing arm observed red — then the implementation, then green.
The derivation's first run over the shipped corpus found a real dead key:
scene2d declared `STYLE.faint` ("not-yet-active linework"), read by
nothing, value hardcoded nowhere. Deleted; all 8 scenes then check
warning-clean, which is the gate.

Two self-catches worth narrating: the breakdown.md provenance amendment
first claimed "the 2D template's declared-key list below shrank by one" —
breakdown has no such list, caught by grepping before commit. And the
measurement-assertion ratchet fired on the new check comment ("measured
2026-08-04" naming no control) — resolved by rewording to the mechanism
rather than the measurement, since the self-announcing property makes the
dated claim unnecessary in code; the dated scan lives in the CHANGELOG
entry, where dated records belong.

Verification at landing (this machine, fallback backend, 2026-08-04):
bracket-commands 40/40, bracket-check-kit 6/6, bracket-corpus 3/3,
parity 9 files / 5,796 held lines, selfcheck green, all 8 scenes
warning-clean. Docs: breakdown's STYLE/CONFIG verdicts closed with a
dated provenance amendment, instruments' table gained the row, plan.md
carries the MET stamp, router and site card updated at the landing.

## Evening: the owner asked for a brainstorm, and the verification pass paid twice

`docs/brainstorm-2026-08-04.md` — the state of the board with every claim
re-derived: the path question (film vs REP4 vs Phase 4, lean stated), the
verified gaps (VISION's determinism-magnitude criterion has no owning
queue item anywhere; the 2026-08-24 muted-blocks count exists only in the
local handoff), the drift classes with their next likely strikes, the
over-implementation look (mostly survived it; bracket-noise's standing
local red is the one ergonomics candidate), and the future problems
(install-cache growth just changed slope with daily releases; REP5 is
film-starved by construction). Graduate-or-die rule borrowed from the
restructure plan: the file deletes itself when every item is
dispositioned. Router row added beside working-plan's.

The verification pass found breakdown.md's "Not here" edge 325 bytes from
selfcheck's header window — the same class that bit instruments.md this
morning — and moved it preemptively, which is 0.19.1 (a structure-only
cascade; invariant 2 does not care that it was only a comment reorder).
Also confirmed zero new example films since the 2026-07-24 migration
(git log --diff-filter=A over examples/), which became the brainstorm's
opening fact, and caught an uncommitted .gitignore edit from another
session before git add -A swept it into a release commit — unstaged,
left in place, logged in the brainstorm as the etiquette's incident entry.

## The next-session handoff convention, retired

Owner asked whether the handoff memo was more harmful than helpful; the
record said harmful, three ways: it rotted fastest of anything here (its
own header admitted four rots in one day), it failed spine rule 0
(gitignored, unreachable from any clone, unnamed in CLAUDE.md — this very
session had to be told it existed), and today's brainstorm sweep caught it
holding an obligation that existed nowhere tracked (the 2026-08-24
muted-blocks count). Owner agreed; retired.

The retirement was precise, and the verification mattered: this repo has
TWO handoff conventions. The next-session memo died; the FILM handoff
report — "names what its author built twice", pattern-ledger's input,
load-bearing for REP5 — lives, and every tracked mention of it
(plan.md, pattern-ledger.md, extract-patterns) was checked and left
alone. The memo's one unique fact graduated to working-plan's deferred
table; the brainstorm's dispositions were updated in both renderings;
the memory notes now say "do not recreate". Orientation from here:
docs/README's work-next row plus this log — the tracked, guarded path,
which is also VISION's cold-start criterion getting a free test every
session.

## Plain-language renderings, and the film field report gets a name

Owner asked for both HTML renderings (the orientation postmortem and the
brainstorm) in plain English. Both rewritten under the house
plain-language style: same findings, numbers and citations, simpler
sentences, no em dashes, bold only for labels. Each page now states that
its markdown is the record and wins any disagreement.

Mid-rewrite the owner asked what "the film handoff report" even was —
the convention this morning's retirement carefully preserved. Fair
question with an embarrassing answer: it had one real instance (the
2026-07-25 circus-build field report), three different names across the
docs, and no definition anywhere findable. Renamed to "film field
report" in the live docs (pattern-ledger.md, which now defines it;
plan.md's two mentions; extract-patterns' SKILL), with the old name
noted at the definition so older records still resolve. Dated records
keep their original wording. The definition, one sentence: after
building a film, the builder writes up what they had to build twice or
re-derive, and those write-ups are what the pattern ledger counts.

## The film field report becomes a method step — the two loops named

Owner asked whether the film field report differs from the harness we
have been building. It does, and naming the difference was the solve:
the harness is the VERIFICATION loop (brackets, selfcheck, parity,
check — keeps what exists correct, defect-driven, mechanical); the field
report feeds the GROWTH loop (build → record what you built twice →
ledger counts recurrence → promotion triggers fire — decides what should
exist next, judgment-laden, cannot be a grep). They meet at one point: a
promotion exits the growth loop and lands inside the harness loop, chart
tier and brackets first.

The mismatch's root, verified before fixing: the convention had no
producer. method.md never asked the builder for anything at ship time,
SKILL.md never mentioned it, and the one report that exists (2026-07-25)
was hand-arranged. Fixed as 0.19.2: method.md ends with "The last step
of a film" (three bullets: built twice, re-derived, copied from an
example — self-contained, no docs/ links, invariant 3 safe), its heading
map gains the section and sheds its hand-written "Six top-level
sections" count (the map's own rot anecdote, applied to itself).
pattern-ledger.md now states form-follows-record and names
/extract-patterns as the executable consumer. VISION's "the mechanism is
unbuilt" was half-stale — extraction shipped as extract-patterns and
nobody updated it — corrected to half-built, with the honest remainder
stated: the loop turning is unproven, and only films can prove it.
